---
phase: 05-prd-ui-multi-shelf-experience
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/lib/shelfSwipe.ts
  - src/lib/shelfSwipe.test.ts
  - src/lib/carouselEnabled.ts
  - src/components/ShelfCarousel.tsx
  - src/components/ShelfCarousel.test.tsx
  - src/components/CameraView.tsx
  - src/App.tsx
  - src/App.shelfIsolation.integration.test.tsx
autonomous: true
requirements:
  - SHLF-01
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - User swipes left/right on camera viewport to switch among 5 shelves with 50px horizontal threshold (D-01, SHLF-01)
    - Swipe calls handleShelfChange preserving Phase 2 storage path (D-02)
    - Centered 5-dot strip below top bar shows active shelf with emerald fill and aria-current (D-03)
    - motion AnimatePresence cross-fades shelf feed on activeShelfId change (~300ms spring, reduced-motion fallback) (D-04)
    - Swipe disabled when appMode is not CAMERA_IDLE or INITIAL_GUIDE (D-05)
    - Shelf switch reloads ghost and baseline per Phase 3 hasPersistedBaseline rules (D-06)
  artifacts:
    - src/components/ShelfCarousel.tsx
    - src/lib/shelfSwipe.ts
    - src/components/ShelfCarousel.test.tsx
    - src/lib/shelfSwipe.test.ts
  key_links:
    - ShelfCarousel onShelfChange → App handleShelfChange
    - carouselEnabled guard from appMode passed to swipe layer
    - motion key={activeShelfId} wraps camera feed container
  prohibitions:
    - Must not add new storage API for shelf switching
    - Must not keep segmented ShelfSelector in top bar
---

# Plan 05-03: Tracer — Shelf Swipe Carousel End-to-End

**As a** retail field rep inspecting multiple shelves,
**I want to** swipe between 5 shelves with a clear active indicator,
**so that** each shelf loads its own ghost overlay and baseline without using top-bar buttons.

<objective>
Wire the production-quality tracer for SHLF-01: horizontal swipe carousel with 5-dot indicator, motion cross-fade, and Phase 2 handleShelfChange integration — one shelf-switch path verified end-to-end before camera chrome polish.

Purpose: Core Phase 5 UX bet per ROADMAP success criterion 1; replaces temporary ShelfSelector per D-01.
Output: ShelfCarousel, shelfSwipe helper, motion feed transition, updated integration tests.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-prd-ui-multi-shelf-experience/05-UI-SPEC.md
@.planning/phases/05-prd-ui-multi-shelf-experience/05-RESEARCH.md
@.planning/phases/03-guided-capture-quality/03-PATTERNS.md
@src/App.tsx
@src/components/CameraView.tsx
@src/components/ShelfSelector.tsx
@src/App.shelfIsolation.integration.test.tsx
</context>

<tasks>

<task type="tracer">
  <name>End-to-end shelf swipe — one switch path through App → storage → ghost (D-01, D-02, D-03, D-04, D-05, D-06, SHLF-01)</name>
  <files>src/lib/shelfSwipe.ts, src/lib/carouselEnabled.ts, src/components/ShelfCarousel.tsx, src/components/CameraView.tsx, src/App.tsx</files>
  <read_first>
    src/App.tsx
    src/components/CameraView.tsx
    .planning/phases/05-prd-ui-multi-shelf-experience/05-RESEARCH.md
    .planning/phases/03-guided-capture-quality/03-PATTERNS.md
  </read_first>
  <reversibility rating="costly">CameraView gesture layer and carousel layout become PRD contract per D-01</reversibility>
  <action>
    Create src/lib/carouselEnabled.ts exporting isCarouselEnabled(appMode) returning true only for CAMERA_IDLE and INITIAL_GUIDE per D-05.

    Create src/lib/shelfSwipe.ts with attachShelfSwipe(el, { enabled, onSwipeLeft, onSwipeRight }) — 50px threshold, ignore vertical-dominant gestures (abs(dy) > abs(dx) returns early) per D-01. Export SWIPE_THRESHOLD_PX = 50 constant.

    Create src/components/ShelfCarousel.tsx: role=tablist, data-testid=shelf-carousel, 5 buttons with data-shelf-index 0–4, aria-current on active, active dot bg-sg-success filled, inactive outline border-sg-border per D-03. Optional shelf label chip via I18N shelfLabel (add minimal keys if missing — full audit in 05-07). Dot click calls onShelfChange when enabled.

    Attach swipe gesture layer on camera viewport (pointer events on dedicated overlay div, not full motion drag — avoids ghost slider conflict per RESEARCH). Swipe left increments shelf (wrap 0–4), swipe right decrements; each calls onShelfChange which must reach App handleShelfChange per D-02.

    In CameraView: remove ShelfSelector from top bar entirely per D-22. Embed ShelfCarousel below top bar row. Wrap feed/ghost container in motion AnimatePresence + motion.div key={activeShelfId} with spring ~300ms; useReducedMotion opacity-only fallback per D-04 discretion.

    In App.tsx: pass carouselEnabled={isCarouselEnabled(appMode)} and activeShelfId to CameraView. Ensure handleShelfChange path unchanged: revokeAll, saveActiveShelfId, loadShelfData per D-02. hasPersistedBaseline updates on switch per D-06.

    Import motion from motion/react. Do not wire INITIAL_GUIDE yet — appMode INITIAL_GUIDE branch comes in 05-04; carouselEnabled already includes INITIAL_GUIDE for forward compatibility.
  </action>
  <verify>
    <automated>grep -c 'data-testid="shelf-carousel"' src/components/ShelfCarousel.tsx | grep -qv '^0$'</automated>
    <fails_when>ShelfCarousel missing test id</fails_when>
    <automated>grep -c 'handleShelfChange\|onShelfChange' src/components/CameraView.tsx src/App.tsx | grep -qv '^0$'</automated>
    <fails_when>Carousel not wired to shelf change handler</fails_when>
    <automated>grep -c 'ShelfSelector' src/components/CameraView.tsx | grep -qx '0'</automated>
    <fails_when>ShelfSelector still imported in CameraView top bar</fails_when>
    <automated>grep -c 'motion/react\|AnimatePresence' src/components/CameraView.tsx src/components/ShelfCarousel.tsx | grep -qv '^0$'</automated>
    <fails_when>motion cross-fade not wired</fails_when>
    <automated>grep -c 'SWIPE_THRESHOLD_PX\|50' src/lib/shelfSwipe.ts | grep -qv '^0$'</automated>
    <fails_when>Swipe threshold helper missing</fails_when>
  </verify>
  <done>One shelf switch via dot tap or swipe reaches handleShelfChange, reloads shelf data, and cross-fades feed; ShelfSelector removed from top bar.</done>
</task>

<task type="auto" tdd="true">
  <name>Carousel and swipe unit tests (D-32, SHLF-01)</name>
  <files>src/components/ShelfCarousel.test.tsx, src/lib/shelfSwipe.test.ts</files>
  <behavior>
    - Active dot at index 2 has aria-current true; others undefined
    - Dot click when enabled calls onShelfChange with correct index
    - Swipe dx=60 horizontal triggers onSwipeLeft; dx=-60 triggers onSwipeRight
    - Vertical-dominant gesture (dy=80, dx=10) does not trigger swipe
    - enabled=false ignores pointer events
  </behavior>
  <action>
    Create ShelfCarousel.test.tsx with render/screen/fireEvent. Assert aria-current on active data-shelf-index. Create shelfSwipe.test.ts attaching helper to div element; simulate pointerDown/Up with clientX deltas per RESEARCH test helper pattern.

    Use bun run test pattern from Phase 3.
  </action>
  <verify>
    <automated>bun run test -- src/components/ShelfCarousel.test.tsx src/lib/shelfSwipe.test.ts</automated>
    <fails_when>Carousel or swipe unit tests fail</fails_when>
  </verify>
  <done>Unit tests prove dot active state and 50px swipe threshold behavior.</done>
</task>

<task type="auto">
  <name>Update shelf isolation integration test for carousel (D-06, D-33)</name>
  <files>src/App.shelfIsolation.integration.test.tsx</files>
  <action>
    Update src/App.shelfIsolation.integration.test.tsx: replace ShelfSelector button clicks (aria-label Shelf 2) with carousel dot selection via data-shelf-index or swipeHorizontal helper (pointerDown at x=100, pointerUp at x=160 for left swipe per RESEARCH).

    Retain ghost overlay src swap assertions from Phase 3 — prove shelf B ghost differs from shelf A after switch via swipe/dot path per D-06.

    Add assertion that shelf-carousel renders with 5 dots.
  </action>
  <verify>
    <automated>bun run test -- src/App.shelfIsolation.integration.test.tsx</automated>
    <fails_when>Shelf isolation integration test fails after carousel migration</fails_when>
    <automated>grep -c 'shelf-carousel\|data-shelf-index' src/App.shelfIsolation.integration.test.tsx | grep -qv '^0$'</automated>
    <fails_when>Integration test still uses removed ShelfSelector selectors</fails_when>
  </verify>
  <done>Integration test proves cross-shelf ghost isolation via carousel interaction path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gesture layer → handleShelfChange | Pointer events on viewport; shelf index bounded 0–4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Denial of Service | shelfSwipe.ts | low | accept | Passive listeners where preventDefault not needed |
| T-05-04 | Tampering | onShelfChange index | medium | mitigate | Clamp shelf index 0–4 before handleShelfChange; no user-supplied shelf id from URL |
</threat_model>

<verification>
bun run test -- ShelfCarousel shelfSwipe App.shelfIsolation passes; grep confirms ShelfSelector removed from CameraView.
</verification>

<success_criteria>
SHLF-01 satisfied: 5-shelf swipe carousel with active indicator wired to existing storage lifecycle.
</success_criteria>

<output>
Create `.planning/phases/05-prd-ui-multi-shelf-experience/05-03-SUMMARY.md` when done
</output>
