---
phase: 05-prd-ui-multi-shelf-experience
plan: 06
type: execute
wave: 6
depends_on:
  - 05-05
files_modified:
  - src/components/RoiSetupView.tsx
  - src/components/ResultInspectView.tsx
  - src/components/ResultInspectView.anomaly.test.tsx
autonomous: true
requirements:
  - DSGN-01
  - DSGN-02
  - DSGN-03
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - ROI setup and Result inspect use light #F8FAFC/#FFFFFF backgrounds (D-15, DSGN-01)
    - Floating controls use glass-panel pattern uniformly (D-14, DSGN-03)
    - MISSING anomalies use #EF4444 2px border + 15% fill; MOVED use #F59E0B (D-16, DSGN-02)
    - Stat capsule dots match anomaly semantic hues (D-16, D-17)
    - Typography uses sg-primary/sg-secondary tokens (D-17)
  artifacts:
    - src/components/ResultInspectView.anomaly.test.tsx
  key_links:
    - ResultInspectView box styles use border-sg-danger bg-sg-danger/15 and border-sg-warning bg-sg-warning/15
    - RoiSetupView wrapper uses bg-sg-surface
  prohibitions:
    - Must not change anomaly detection logic or worker contracts
    - Must not alter ROI divider interaction contracts from Phase 4
---

# Plan 05-06: Light Theme Views + Anomaly State Colors

<objective>
Apply Minimalist Light design tokens to ROI setup and Result inspect views with DSGN-02 anomaly overlay colors and DSGN-03 glassmorphism on floating controls per D-14 through D-17.

Purpose: ROADMAP success criterion 3 — all three views match Minimalist Light tokens.
Output: Themed RoiSetupView and ResultInspectView, anomaly color tests.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-prd-ui-multi-shelf-experience/05-UI-SPEC.md
@.planning/phases/04-real-inspection-pipeline/04-UI-SPEC.md
@src/components/RoiSetupView.tsx
@src/components/ResultInspectView.tsx
@src/lib/designTokens.ts
</context>

<tasks>

<task type="auto">
  <name>RoiSetupView light theme + glass controls (D-14, D-15, DSGN-01, DSGN-03)</name>
  <files>src/components/RoiSetupView.tsx</files>
  <action>
    Update RoiSetupView root wrapper to bg-sg-surface (#F8FAFC) per D-15. Sticky header and bottom drawer use bg-sg-white per Minimalist Light PRD.

    Apply glass-panel utility to floating control clusters (tier quick-select container, bottom action row pills where appropriate) per D-14. Replace scattered arbitrary hex in touched sections with sg-primary, sg-secondary, sg-border token utilities per D-13 and D-17.

    Preserve Phase 4 ROI interaction contracts unchanged — divider drag, magnifier 80px, retake/save buttons behavior from 04-UI-SPEC. Polish-in-place per D-20; no layout rewrite unless spacing cannot meet PRD within ±4px discretion.
  </action>
  <verify>
    <automated>grep -c 'bg-sg-surface\|bg-sg-white' src/components/RoiSetupView.tsx | grep -qv '^0$'</automated>
    <fails_when>ROI view missing light theme backgrounds</fails_when>
    <automated>grep -c 'glass-panel' src/components/RoiSetupView.tsx | grep -qv '^0$'</automated>
    <fails_when>Glassmorphism pattern not applied to ROI controls</fails_when>
  </verify>
  <done>ROI setup view uses Minimalist Light backgrounds and glass floating controls.</done>
</task>

<task type="auto">
  <name>ResultInspectView DSGN-02 anomaly colors + light chrome (D-16, D-17, DSGN-02)</name>
  <files>src/components/ResultInspectView.tsx</files>
  <action>
    Update ResultInspectView wrapper to bg-sg-surface / bg-sg-white header pattern per D-15.

    Update anomaly box styles per D-16: MISSING → border-2 border-sg-danger bg-sg-danger/15 rounded-xl; MOVED → border-2 border-sg-warning bg-sg-warning/15 rounded-xl. Use SG_COLORS from designTokens.ts for any inline style if Tailwind utilities insufficient.

    Update stat capsule filter chips: active missing chip bg-sg-danger; active displaced bg-sg-warning; dots match same hues per D-16. Typography text-sg-primary for headings, text-sg-secondary for labels per D-17.

    Apply glass-panel to stat capsule bar container and tolerance drawer panel per D-14. Keep Phase 4 interaction contracts (blink compare, tap dismiss, slider debounce) unchanged.
  </action>
  <verify>
    <automated>grep -c 'sg-danger\|sg-warning' src/components/ResultInspectView.tsx | grep -qv '^0$'</automated>
    <fails_when>Anomaly token colors not applied</fails_when>
    <automated>grep -c 'bg-sg-surface\|bg-sg-white' src/components/ResultInspectView.tsx | grep -qv '^0$'</automated>
    <fails_when>Result view missing light theme backgrounds</fails_when>
  </verify>
  <done>Result inspect shows PRD anomaly colors and light theme chrome.</done>
</task>

<task type="auto" tdd="true">
  <name>Anomaly overlay color component tests (D-32, DSGN-02)</name>
  <files>src/components/ResultInspectView.anomaly.test.tsx</files>
  <behavior>
    - MISSING anomaly box renders border-sg-danger and bg-sg-danger/15 classes
    - MOVED anomaly box renders border-sg-warning and bg-sg-warning/15 classes
    - Stat capsule missing chip uses sg-danger hue when count > 0
  </behavior>
  <action>
    Create ResultInspectView.anomaly.test.tsx with minimal fixture anomalies (one MISSING, one MOVED). Render ResultInspectView with required props from existing test patterns. Assert class names contain sg-danger/sg-warning tokens on box elements.

    Run bun run lint after token migration in both views.
  </action>
  <verify>
    <automated>bun run test -- src/components/ResultInspectView.anomaly.test.tsx</automated>
    <fails_when>Anomaly color tests fail</fails_when>
    <automated>bun run lint</automated>
    <fails_when>Lint fails after view theme changes</fails_when>
  </verify>
  <done>Automated tests confirm DSGN-02 anomaly color contract on ResultInspectView.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view styling → anomaly data | Presentation only; no change to vision worker output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Tampering | anomaly box styles | low | accept | Visual layer only; underlying anomaly ids unchanged |
</threat_model>

<verification>
bun run test -- ResultInspectView.anomaly; grep confirms sg-* tokens in ROI and Result views.
</verification>

<success_criteria>
DSGN-01, DSGN-02, DSGN-03 satisfied on ROI and Result views.
</success_criteria>

<output>
Create `.planning/phases/05-prd-ui-multi-shelf-experience/05-06-SUMMARY.md` when done
</output>
