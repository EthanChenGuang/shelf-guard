---
phase: 05-prd-ui-multi-shelf-experience
plan: 05
type: execute
wave: 5
depends_on:
  - 05-04
files_modified:
  - src/components/CameraView.tsx
  - src/components/CameraView.topBar.test.tsx
  - src/components/ScanningAnimationOverlay.tsx
  - src/lib/constants.ts
autonomous: true
requirements:
  - CAM-04
  - CAM-05
  - CAM-06
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Top bar shows baseline pill left, level badge center, torch + language right (D-21, CAM-04)
    - Segmented shelf selector, demo toggle, and PWA removed from top bar (D-22)
    - Demo/Cam toggle relocated to bottom-left utility pill below carousel dots (D-23)
    - Baseline pill tap opens ResetBaselineModal flow (D-24)
    - 76px double-ring shutter with breathing glow when ready (D-25, CAM-05)
    - 48×48 rounded-xl last-inspection thumbnail bottom-right (D-26, CAM-05)
    - Scan beam 0.8s cyan polish without duration change (D-27, CAM-06)
    - Shutter tap white flash overlay for capture feedback (D-28)
  artifacts:
    - src/components/CameraView.topBar.test.tsx
  key_links:
    - Baseline pill shows established vs not-established copy from I18N
    - Demo utility pill uses t.useSampleFeed / t.useRealCamera
    - ScanningAnimationOverlay retains 800ms animate-scan-beam
  prohibitions:
    - Must not change scan animation duration from 800ms
    - Must not re-add ShelfSelector to top bar
---

# Plan 05-05: Camera PRD Chrome — Top Bar, Shutter, Scan

<objective>
Refactor CameraView to PRD 3-zone top bar, breathing 76px shutter with thumbnail, demo utility relocation, and scan-line polish per D-21 through D-28, CAM-04, CAM-05, and CAM-06.

Purpose: ROADMAP success criterion 4 — camera view matches PRD visual signature.
Output: PRD top bar layout, shutter/thumbnail polish, enhanced scan overlay, top bar tests.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-prd-ui-multi-shelf-experience/05-UI-SPEC.md
@.planning/phases/05-prd-ui-multi-shelf-experience/05-RESEARCH.md
@src/components/CameraView.tsx
@src/components/ScanningAnimationOverlay.tsx
@src/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>PRD 3-zone top bar refactor (D-21, D-22, D-23, D-24, CAM-04)</name>
  <files>src/components/CameraView.tsx, src/lib/constants.ts</files>
  <reversibility rating="costly">Top bar becomes visual signature per D-21</reversibility>
  <action>
    Refactor CameraView top floating bar to fixed 3-zone grid per 05-UI-SPEC and D-21:

    Left zone — baseline status pill: glass-panel styling via token utilities. When hasPersistedBaseline show t.baselineEstablished with green pulse dot; when false show t.baselineNotSet without pulse per D-21. onClick remains onResetBaselinePrompt (ResetBaselineModal) per D-24.

    Center zone — level micro-badge: move compact tilt display from crosshair overlay (0.0° mint bg-sg-success/10 when isLevel, amber when tilted). Preserve desktop simulate toggle guard from Phase 3.

    Right zone — torch (when hasTorch && !isUsingDemoFeed) + language toggle 中/EN only per D-21. Remove PWA install button from top bar per D-22 (omit or defer to browser prompt).

    Relocate Demo/Cam toggle to bottom-left utility pill below ShelfCarousel dots per D-23. Use glass-panel small pill with I18N t.useSampleFeed / t.useRealCamera — no hardcoded Demo/Cam strings.

    Add I18N keys baselineNotSet, baselineEstablished states if not present per D-30 partial. Apply sg-* token classes replacing arbitrary hex in refactored bar sections per D-13 migration scope for this file.
  </action>
  <verify>
    <automated>grep -c 'baselineNotSet\|baselineEstablished' src/lib/constants.ts | grep -qv '^0$'</automated>
    <fails_when>Baseline pill I18N keys missing</fails_when>
    <automated>grep -c 'useSampleFeed\|useRealCamera' src/components/CameraView.tsx | grep -qv '^0$'</automated>
    <fails_when>Demo utility not using I18N labels</fails_when>
    <automated>grep -c 'ShelfSelector' src/components/CameraView.tsx | grep -qx '0'</automated>
    <fails_when>ShelfSelector reintroduced to top bar</fails_when>
  </verify>
  <done>Top bar matches PRD 3-zone layout; demo toggle at bottom-left; baseline pill reflects persisted state.</done>
</task>

<task type="auto">
  <name>76px breathing shutter + 48×48 thumbnail + flash (D-25, D-26, D-28, CAM-05)</name>
  <files>src/components/CameraView.tsx, src/index.css</files>
  <action>
    Update shutter button to 76px outer ring with double-ring styling per 05-UI-SPEC. Apply shutter-breathe keyframe (scale 0.98–1.02) + outer ring animate-pulse when appMode CAMERA_IDLE, !isShutterLocked, and camera available per D-25. Disable breathing during scan, processing, locked states.

    Resize last-inspection thumbnail to 48×48 rounded-xl with border-sg-border per D-26. Position bottom-right relative to 76px shutter in existing bottom bar — PRD spacing ±4px discretion.

    Add brief white flash overlay on shutter tap (opacity fade ~150ms) if not already present per D-28. Use local useState flashVisible cleared by timeout.

    Wire thumbnail tap to existing history modal handler unchanged.
  </action>
  <verify>
    <automated>grep -c 'w-\[76px\]\|h-\[76px\]\|76px' src/components/CameraView.tsx | grep -qv '^0$'</automated>
    <fails_when>76px shutter sizing missing</fails_when>
    <automated>grep -c 'w-12 h-12\|48px\|w-\[48px\]' src/components/CameraView.tsx | grep -qv '^0$'</automated>
    <fails_when>48px thumbnail sizing missing</fails_when>
    <automated>grep -c 'shutter-breathe\|animate-pulse' src/components/CameraView.tsx src/index.css | grep -qv '^0$'</automated>
    <fails_when>Shutter breathing animation not defined</fails_when>
  </verify>
  <done>Shutter and thumbnail match PRD measurements with breathing glow when ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Scan line polish + top bar tests (D-27, D-32, CAM-06, CAM-04)</name>
  <files>src/components/ScanningAnimationOverlay.tsx, src/components/CameraView.topBar.test.tsx</files>
  <behavior>
    - ScanningAnimationOverlay retains 0.8s duration class or style
    - Baseline pill shows established text when hasPersistedBaseline true
    - Baseline pill shows not-set text when hasPersistedBaseline false
    - Language toggle renders CN or EN label from lang prop
  </behavior>
  <action>
    Polish ScanningAnimationOverlay: strengthen cyan #38BDF8 gradient/glow per D-27. Do not change 800ms duration or animate-scan-beam timing (STAB-01/03). Optional motion enhancement only if CSS insufficient per D-27 discretion.

    Create CameraView.topBar.test.tsx with baseProps fixture. Test baseline pill copy for hasPersistedBaseline true/false. Test lang cn/en on language button. Assert scan overlay duration grep in separate unit if overlay exported.

    Migrate remaining arbitrary hex in CameraView touched sections to sg-* tokens where feasible per D-13 partial scope.
  </action>
  <verify>
    <automated>grep -c '0\.8s\|800ms\|animate-scan-beam' src/components/ScanningAnimationOverlay.tsx | grep -qv '^0$'</automated>
    <fails_when>Scan duration changed or removed</fails_when>
    <automated>bun run test -- src/components/CameraView.topBar.test.tsx</automated>
    <fails_when>Top bar component tests fail</fails_when>
  </verify>
  <done>Scan beam polished at 0.8s; top bar tests cover baseline states and language display.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| camera chrome → device APIs | Torch and camera remain gated by existing permission flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Information Disclosure | baseline pill | low | accept | Pill reflects local persisted state only |
| T-05-08 | Tampering | scan duration | medium | mitigate | grep gate enforces 800ms; STAB contract preserved |
</threat_model>

<verification>
bun run test -- CameraView.topBar; grep confirms 76px shutter and 800ms scan; top bar 3-zone layout without ShelfSelector.
</verification>

<success_criteria>
CAM-04, CAM-05, CAM-06 satisfied: PRD camera chrome complete on CameraView.
</success_criteria>

<output>
Create `.planning/phases/05-prd-ui-multi-shelf-experience/05-05-SUMMARY.md` when done
</output>
