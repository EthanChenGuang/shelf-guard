---
phase: 05-prd-ui-multi-shelf-experience
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - package.json
  - package-lock.json
  - src/index.css
  - src/lib/designTokens.ts
autonomous: false
requirements:
  - DSGN-01
  - DSGN-03
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "PRD color tokens defined once in @theme and mirrored in designTokens.ts (D-13, DSGN-01)"
    - glass-panel utility class available for floating controls (D-14, DSGN-03)
    - "motion@^13.4.2 installed and importable from motion/react for carousel animations (D-04)"
  artifacts:
    - src/index.css
    - src/lib/designTokens.ts
    - package.json
  key_links:
    - "@theme --color-sg-* generates bg-sg-success, text-sg-primary utilities"
    - "SG_COLORS in designTokens.ts matches @theme hex values for JS/canvas use"
  prohibitions:
    - Must not change scan animation duration (800ms STAB contract)
---

# Plan 05-02: Design Tokens + Motion Dependency Foundation

<objective>
Centralize Minimalist Light PRD tokens and install the motion library so carousel and transition tasks in 05-03+ use named utilities instead of scattered arbitrary hex per D-13, DSGN-01, and DSGN-03.

Purpose: Token foundation before visual refactors; motion dependency required for D-04 shelf cross-fade.
Output: @theme block, designTokens.ts, glass-panel utility, motion package installed.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-prd-ui-multi-shelf-experience/05-UI-SPEC.md
@.planning/phases/05-prd-ui-multi-shelf-experience/05-RESEARCH.md
@src/index.css
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Verify motion package legitimacy before install (D-04, T-05-SC)</name>
  <what-built>Phase 5 dependency gate — motion library required for shelf carousel cross-fade per D-04.</what-built>
  <how-to-verify>
    1. Open npmjs.com/package/motion in browser.
    2. Confirm publisher is Motion Division and repository link is github.com/motiondivision/motion.
    3. Confirm weekly download count is high (official package, not typosquat).
    4. RESEARCH.md flags [SUS] due to recent publish date — verify repo matches motiondivision/motion before approving install.
  </how-to-verify>
  <resume-signal>Type "approved" if motion package verified on npm registry, or describe concerns for gap closure.</resume-signal>
</task>

<task type="auto">
  <name>Install motion and add PRD @theme tokens (D-04, D-13, DSGN-01)</name>
  <files>package.json, package-lock.json, src/index.css, src/lib/designTokens.ts</files>
  <reversibility rating="costly">Token names and motion dependency become cross-phase UI contract</reversibility>
  <action>
    Run bun add motion@^13.4.2 (or npm install motion@^13.4.2 if bun unavailable). Verify import path motion/react works in a smoke comment or leave for 05-03.

    In src/index.css @theme block per D-13 and 05-UI-SPEC token table, add: --color-sg-white #ffffff, --color-sg-surface #f8fafc, --color-sg-primary #0f172a, --color-sg-secondary #64748b, --color-sg-border #e2e8f0, --color-sg-success #10b981, --color-sg-danger #ef4444, --color-sg-warning #f59e0b, --color-sg-camera #0f172a, --color-sg-scan #38bdf8.

    Create src/lib/designTokens.ts exporting SG_COLORS object with same hex values for JS access (anomaly overlays, canvas if needed).

    Add @utility glass-panel per D-14: rounded-2xl backdrop-blur-md bg-white/75 shadow-md border border-sg-border/60.

    Add @keyframes shutter-breathe for later CAM-05 use (scale 0.98–1.02) — define only, do not wire to components yet.

    Do not migrate component hex in this task — token foundation only.
  </action>
  <verify>
    <automated>grep -c 'motion' package.json | grep -qv '^0$'</automated>
    <fails_when>motion not in package.json dependencies</fails_when>
    <automated>grep -c 'color-sg-success' src/index.css | grep -qv '^0$'</automated>
    <fails_when>@theme PRD tokens missing</fails_when>
    <automated>test -f src/lib/designTokens.ts && grep -c 'SG_COLORS' src/lib/designTokens.ts | grep -qv '^0$'</automated>
    <fails_when>designTokens.ts missing SG_COLORS export</fails_when>
    <automated>bun run lint</automated>
    <fails_when>TypeScript fails after new files</fails_when>
  </verify>
  <done>motion installed; @theme tokens and SG_COLORS mirror PRD palette; glass-panel utility defined.</done>
</task>

<task type="auto">
  <name>Verify build with motion tree-shaking (DSGN-03)</name>
  <files>package.json</files>
  <action>
    Run bun run build to confirm motion dependency does not break production bundle. No component imports motion yet — build must pass with tokens-only changes.
  </action>
  <verify>
    <automated>bun run build</automated>
    <fails_when>Production build fails after motion install</fails_when>
  </verify>
  <done>Build green with motion in dependencies and PRD tokens in CSS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → package.json | Untrusted third-party code enters dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | npm install motion | high | mitigate | blocking-human checkpoint verifies motiondivision/motion repo before install |
| T-05-02 | Tampering | designTokens.ts | low | accept | Static hex constants; no user input |
</threat_model>

<verification>
grep confirms @theme tokens; bun run lint and bun run build pass.
</verification>

<success_criteria>
PRD token foundation and motion dependency ready for carousel tracer in 05-03.
</success_criteria>

<output>
Create `.planning/phases/05-prd-ui-multi-shelf-experience/05-02-SUMMARY.md` when done
</output>
