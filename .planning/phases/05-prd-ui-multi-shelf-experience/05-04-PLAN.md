---
phase: 05-prd-ui-multi-shelf-experience
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - src/components/InitialGuideOverlay.tsx
  - src/components/InitialGuideOverlay.test.tsx
  - src/components/CameraView.tsx
  - src/App.tsx
  - src/App.initialGuide.integration.test.tsx
  - src/lib/constants.ts
autonomous: true
requirements:
  - SHLF-05
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Empty shelf enters INITIAL_GUIDE on app init, shelf switch, or return from audit (D-07, SHLF-05)
    - Full-screen 3-step overlay on camera shell with capture baseline CTA and skip (D-08, D-11)
    - No DEFAULT_CALIBRATION demo image shown as established baseline in guide state (D-09)
    - After guide dismiss, shutter follows Phase 4 first-baseline ROI_CONFIG path (D-10)
    - Skip lands on CAMERA_IDLE without persisting skip preference (D-11)
    - Shelf with persisted baseline never shows INITIAL_GUIDE until reset (D-12)
  artifacts:
    - src/components/InitialGuideOverlay.tsx
    - src/components/InitialGuideOverlay.test.tsx
    - src/App.initialGuide.integration.test.tsx
  key_links:
    - resolveAppModeAfterShelfLoad sets INITIAL_GUIDE when !hasPersistedBaseline
    - CameraView renders InitialGuideOverlay when showInitialGuide prop true
    - onDismissInitialGuide sets CAMERA_IDLE
  prohibitions:
    - Must not persist skip preference to IndexedDB
    - Must not render silent demo baseline during INITIAL_GUIDE
---

# Plan 05-04: INITIAL_GUIDE Per-Shelf Onboarding

<objective>
Wire INITIAL_GUIDE AppMode for shelves without persisted baseline — full-screen 3-step overlay, skip path, and FSM integration with Phase 4 first-capture contract per D-07 through D-12 and SHLF-05.

Purpose: ROADMAP success criterion 2 — empty shelves get guided setup instead of silent demo baseline.
Output: InitialGuideOverlay, App mode resolution, integration tests.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-prd-ui-multi-shelf-experience/05-UI-SPEC.md
@.planning/phases/05-prd-ui-multi-shelf-experience/05-RESEARCH.md
@.planning/phases/04-real-inspection-pipeline/04-UI-SPEC.md
@src/App.tsx
@src/types.ts
@src/components/CameraView.tsx
</context>

<tasks>

<task type="tracer">
  <name>End-to-end INITIAL_GUIDE — empty shelf → overlay → dismiss → CAMERA_IDLE (D-07, D-08, D-09, D-10, D-11, D-12, SHLF-05)</name>
  <files>src/App.tsx, src/components/InitialGuideOverlay.tsx, src/components/CameraView.tsx, src/lib/constants.ts</files>
  <read_first>
    src/App.tsx
    src/types.ts
    .planning/phases/04-real-inspection-pipeline/04-UI-SPEC.md
    .planning/phases/05-prd-ui-multi-shelf-experience/05-CONTEXT.md
  </read_first>
  <reversibility rating="costly">FSM entry conditions become UX contract with Phase 4 first-capture path per D-07</reversibility>
  <action>
    Add resolveAppModeAfterShelfLoad(hasBaseline: boolean, currentMode: AppMode): AppMode in App.tsx per RESEARCH Pattern 3. When !hasBaseline return INITIAL_GUIDE; when baseline exists and currentMode is INITIAL_GUIDE return CAMERA_IDLE; otherwise preserve non-idle modes per D-07.

    Call resolveAppModeAfterShelfLoad at end of loadShelfData after setHasPersistedBaseline. On app init after first loadShelfData, apply same resolution. After handleCompleteAudit return to camera on empty shelf, re-resolve per D-07. After handleResetToDefault clears baseline, set INITIAL_GUIDE per D-12 inverse.

    Extend App render: appMode === 'CAMERA_IDLE' || appMode === 'INITIAL_GUIDE' share CameraView shell. Pass showInitialGuide={appMode === 'INITIAL_GUIDE'}, onDismissInitialGuide={() => setAppMode('CAMERA_IDLE')}.

    Create InitialGuideOverlay.tsx: data-testid=initial-guide, absolute inset-0 z-40, bg-sg-camera/95. Three steps via useState(step): (0) welcome + shelf name from I18N, (1) alignment tips noting ghost/level apply after baseline, (2) primary CTA captureBaseline (拍摄基准图 / Capture baseline) calls onComplete → CAMERA_IDLE, secondary skipGuide (跳过 / Skip) calls onSkip → CAMERA_IDLE per D-08, D-11. Step indicator styling at executor discretion per D-32 discretion.

    In CameraView when showInitialGuide: render InitialGuideOverlay; force neutral dark placeholder for feed — do not show DEFAULT_CALIBRATION CDN image as if established baseline per D-09. Live camera may show if already enabled. Hide ghost overlay during guide (!hasPersistedBaseline already gates ghost).

    Verify Phase 4 first-baseline shutter intercept exists: empty shelf after guide dismiss, shutter routes to ROI_CONFIG with pending capture, no scan animation until baseline saved per D-10. If missing, add minimal guard in handleShutterClick checking !hasPersistedBaseline before SCANNING_ANIM — do not replace ROI save contract.

    Add minimal I18N keys for guide steps, captureBaseline, skipGuide, welcomeStep, alignmentStep — full audit deferred to 05-07 but keys needed for overlay must exist per D-30 partial.
  </action>
  <verify>
    <automated>grep -c 'INITIAL_GUIDE' src/App.tsx | grep -qv '^0$'</automated>
    <fails_when>INITIAL_GUIDE mode not assigned in App.tsx</fails_when>
    <automated>grep -c 'resolveAppModeAfterShelfLoad\|showInitialGuide' src/App.tsx | grep -qv '^0$'</automated>
    <fails_when>Mode resolution or guide prop not wired</fails_when>
    <automated>grep -c 'data-testid="initial-guide"' src/components/InitialGuideOverlay.tsx | grep -qv '^0$'</automated>
    <fails_when>InitialGuideOverlay missing test id</fails_when>
    <automated>grep -c 'captureBaseline\|skipGuide' src/lib/constants.ts | grep -qv '^0$'</automated>
    <fails_when>Guide I18N keys missing</fails_when>
  </verify>
  <done>Empty shelf shows INITIAL_GUIDE overlay; complete or skip lands on CAMERA_IDLE; persisted baseline shelf skips guide.</done>
</task>

<task type="auto" tdd="true">
  <name>InitialGuideOverlay component tests (D-32, SHLF-05)</name>
  <files>src/components/InitialGuideOverlay.test.tsx</files>
  <behavior>
    - Renders data-testid initial-guide with step 0 welcome copy
    - Skip button calls onSkip once
    - Final step capture baseline button calls onComplete
    - Both cn and en lang props render translated skipGuide string
  </behavior>
  <action>
    Create InitialGuideOverlay.test.tsx following CameraView.error.test.tsx fixture pattern. Step through 0→1→2 via next controls if present, or render step 2 directly via prop/export for CTA test. Assert I18N cn/en for skipGuide per D-29 partial.
  </action>
  <verify>
    <automated>bun run test -- src/components/InitialGuideOverlay.test.tsx</automated>
    <fails_when>InitialGuideOverlay tests fail</fails_when>
  </verify>
  <done>Component tests cover visibility, skip, and complete callbacks with bilingual copy.</done>
</task>

<task type="auto">
  <name>INITIAL_GUIDE integration test — empty shelf switch (D-33, SHLF-05)</name>
  <files>src/App.initialGuide.integration.test.tsx</files>
  <action>
    Create src/App.initialGuide.integration.test.tsx using fake-indexeddb pattern from App.shelfIsolation.integration.test.tsx. Seed shelf 0 with baseline, leave shelf 1 empty. Switch to shelf 1 via carousel data-shelf-index click. Assert initial-guide overlay present. Switch back to shelf 0 — guide hidden. Assert swipe/carousel still works in INITIAL_GUIDE per D-05 forward compat.

    Test reset baseline on shelf 0 re-shows guide on next camera entry per D-12.
  </action>
  <verify>
    <automated>bun run test -- src/App.initialGuide.integration.test.tsx</automated>
    <fails_when>INITIAL_GUIDE integration test fails</fails_when>
    <automated>grep -c 'initial-guide' src/App.initialGuide.integration.test.tsx | grep -qv '^0$'</automated>
    <fails_when>Integration test missing guide assertions</fails_when>
  </verify>
  <done>Integration test proves empty shelf shows guide and baseline shelf hides it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| I18N strings → overlay DOM | Plain text from constants only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering | InitialGuideOverlay copy | medium | mitigate | Use I18N plain strings only; no dangerouslySetInnerHTML per RESEARCH security domain |
| T-05-06 | Spoofing | DEFAULT_CALIBRATION feed | medium | mitigate | D-09 placeholder blocks demo image masquerading as baseline |
</threat_model>

<verification>
bun run test -- InitialGuideOverlay App.initialGuide passes; grep confirms INITIAL_GUIDE assigned in App.tsx.
</verification>

<success_criteria>
SHLF-05 satisfied: per-shelf INITIAL_GUIDE replaces silent demo baseline onboarding.
</success_criteria>

<output>
Create `.planning/phases/05-prd-ui-multi-shelf-experience/05-04-SUMMARY.md` when done
</output>
