---
phase: 05-prd-ui-multi-shelf-experience
plan: 07
type: execute
wave: 7
depends_on:
  - 05-06
files_modified:
  - src/lib/constants.ts
  - src/components/CameraView.tsx
  - src/components/RoiSetupView.tsx
  - src/components/ResultInspectView.tsx
  - src/components/InitialGuideOverlay.tsx
  - src/components/ShelfCarousel.tsx
  - src/components/I18n.coverage.test.tsx
autonomous: true
requirements:
  - I18N-01
  - I18N-02
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - All user-visible strings in Phase 5 touched components have cn and en I18N entries (D-29, I18N-01)
    - Guide, baseline pill, carousel, demo toggle, top bar strings complete (D-30)
    - Language toggle re-renders new keys immediately without reload (D-31, I18N-02)
    - Swipe disabled during SCANNING_ANIM and other non-idle modes (D-32, D-05)
    - Phase gate test suite green (D-33, D-34 manual UAT doc referenced)
  artifacts:
    - src/components/I18n.coverage.test.tsx
  key_links:
    - saveLanguage/loadSavedLanguage persist lang to IndexedDB shelfguard_lang
    - onLanguageToggle updates lang state and all t.* references re-render
  prohibitions:
    - Must not add hardcoded UI strings in Phase 5 touched files after audit
---

# Plan 05-07: I18N Audit + Phase Verification Gate

<objective>
Complete bilingual copy coverage for all Phase 5 UI surfaces, verify persisted language preference, and run phase automated verification gate per D-29 through D-34, I18N-01, and I18N-02.

Purpose: ROADMAP success criterion 5 — full three-view bilingual copy with persisted preference.
Output: I18N audit complete, coverage test, phase gate green.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-prd-ui-multi-shelf-experience/05-UI-SPEC.md
@.planning/design/stitch/UAT-CHECKLIST.md
@src/lib/constants.ts
@src/lib/storage.ts
@.planning/phases/03-guided-capture-quality/03-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Full I18N audit for Phase 5 surfaces (D-29, D-30, I18N-01)</name>
  <files>src/lib/constants.ts, src/components/CameraView.tsx, src/components/RoiSetupView.tsx, src/components/ResultInspectView.tsx, src/components/InitialGuideOverlay.tsx, src/components/ShelfCarousel.tsx</files>
  <action>
    Audit all user-visible strings in CameraView, RoiSetupView, ResultInspectView, InitialGuideOverlay, ShelfCarousel, and modals opened from these views. Extend I18N in src/lib/constants.ts with cn and en pairs for any missing keys per D-29.

    Ensure complete coverage per D-30: INITIAL_GUIDE step copy (welcomeStep, alignmentStep, captureBaseline, skipGuide), baseline pill states (baselineEstablished, baselineNotSet), shelf carousel (shelfCarouselLabel, shelfLabel function or template), demo toggle (useSampleFeed, useRealCamera), top bar strings, scan/processing pills if hardcoded.

    Replace any remaining hardcoded Chinese or English UI strings in Phase 5 touched files with t.* references. Grep for common patterns: Chinese characters in JSX text nodes, literal Demo/Cam, 跳过, 基准.

    Do not add strings for deferred features (per-shelf language, delete-audit UI).
  </action>
  <verify>
    <automated>grep -c 'captureBaseline\|skipGuide\|shelfCarouselLabel' src/lib/constants.ts | grep -qv '^0$'</automated>
    <fails_when>Phase 5 I18N keys missing from constants</fails_when>
    <automated>grep -c 't\.' src/components/CameraView.tsx src/components/InitialGuideOverlay.tsx src/components/ShelfCarousel.tsx | grep -qv '^0$'</automated>
    <fails_when>Touched components not consuming I18N via t.*</fails_when>
    <automated>grep -c 'shelfCarouselLabel\|captureBaseline\|skipGuide\|baselineNotSet' src/lib/constants.ts | grep -qv '^0$'</automated>
    <fails_when>Required I18N keys missing from constants</fails_when>
  </verify>
  <done>All Phase 5 UI copy sourced from I18N cn/en pairs; no new hardcoded strings in touched files.</done>
</task>

<task type="auto" tdd="true">
  <name>Language toggle re-render + swipe guard tests (D-31, D-32, I18N-02)</name>
  <files>src/components/I18n.coverage.test.tsx, src/components/ShelfCarousel.test.tsx</files>
  <behavior>
    - InitialGuideOverlay renders skipGuide in en when lang=en and 跳过 equivalent when lang=cn
    - ShelfCarousel shelfCarouselLabel aria-label present in both langs
    - carouselEnabled false prevents onShelfChange on dot click (extend ShelfCarousel.test.tsx)
  </behavior>
  <action>
    Create I18n.coverage.test.tsx rendering key Phase 5 components with lang=cn then lang=en; assert translated strings differ for captureBaseline, skipGuide, baselineNotSet.

    Extend ShelfCarousel.test.tsx: when enabled=false, dot click does not call onShelfChange per D-32 swipe-disabled contract.

    Verify App.tsx onLanguageToggle calls saveLanguage and updates lang state — integration-level check via component props if App test too heavy.
  </action>
  <verify>
    <automated>bun run test -- src/components/I18n.coverage.test.tsx src/components/ShelfCarousel.test.tsx</automated>
    <fails_when>I18N coverage or swipe guard tests fail</fails_when>
  </verify>
  <done>Language toggle updates visible copy immediately; carousel respects enabled guard.</done>
</task>

<task type="auto">
  <name>Phase 5 automated verification gate (D-33, D-34, D-06)</name>
  <files>src/App.shelfIsolation.integration.test.tsx, src/App.initialGuide.integration.test.tsx</files>
  <action>
    Run full Phase 5 test subset then full suite gate matching Phase 3 pattern:

    bun run test -- src/components/ShelfCarousel.test.tsx src/lib/shelfSwipe.test.ts src/components/InitialGuideOverlay.test.tsx src/components/CameraView.topBar.test.tsx src/components/ResultInspectView.anomaly.test.tsx src/components/I18n.coverage.test.tsx src/App.shelfIsolation.integration.test.tsx src/App.initialGuide.integration.test.tsx

    Then bun run lint && bun run build && bun run test for phase gate.

    Confirm .planning/design/stitch/UAT-CHECKLIST.md referenced in SUMMARY for D-34 manual Stitch comparison — executor documents UAT pending in SUMMARY, not automated in CI per D-19.
  </action>
  <verify>
    <automated>bun run test -- src/hooks/useDeviceOrientation.test.ts src/components/CameraView.ghost.test.tsx src/components/CameraView.orientation.test.tsx src/App.shelfIsolation.integration.test.tsx src/App.initialGuide.integration.test.tsx src/components/ShelfCarousel.test.tsx src/components/InitialGuideOverlay.test.tsx</automated>
    <fails_when>Phase 5 regression subset fails</fails_when>
    <automated>bun run lint && bun run build && bun run test</automated>
    <fails_when>Full phase gate fails lint, build, or test</fails_when>
  </verify>
  <done>Full test suite green; manual Stitch UAT checklist ready for D-34 sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB lang preference | Client-only storage via idb-keyval |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10 | Tampering | I18N strings | low | accept | Static constants; no runtime injection |
| T-05-11 | Information Disclosure | lang preference | low | accept | Non-sensitive UI preference in IndexedDB |
</threat_model>

<verification>
bun run lint && bun run build && bun run test all green; I18N keys complete for Phase 5 surfaces.
</verification>

<success_criteria>
I18N-01 and I18N-02 satisfied; Phase 5 automated gate green; D-34 manual UAT path documented.
</success_criteria>

<output>
Create `.planning/phases/05-prd-ui-multi-shelf-experience/05-07-SUMMARY.md` when done
</output>
