---
phase: 03-guided-capture-quality
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/hooks/useDeviceOrientation.ts
  - src/hooks/useDeviceOrientation.test.ts
  - src/test/deviceOrientationMocks.ts
  - src/App.tsx
  - src/components/CameraView.tsx
  - src/components/CameraView.orientation.test.tsx
  - src/lib/constants.ts
autonomous: true
requirements:
  - CAM-01
  - CAM-03
  - CAM-07
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Orientation permission requested on same user gesture as live camera enable, after getUserMedia succeeds (D-06, D-10, CAM-07)
    - useDeviceOrientation exports requestOrientationPermission and orientationPermission state; listener attaches only after grant or on non-iOS (D-07)
    - Orientation denied shows inline banner on CameraView with i18n Settings guide and Retry button (D-08, D-09)
    - Level crosshair always visible; frozen at 0° gray styling when permission denied (D-08, D-14, CAM-03)
    - Level snap at ±1.5° gamma with mint green #10B981 and 40ms vibrate retained (D-11)
    - Haptic debounce 1200ms real sensor, 800ms simulate path (D-12)
    - Simulate toggle visible only when !hasSensor && !orientationDenied (D-13)
    - Camera works independently when orientation denied (D-10)
  artifacts:
    - src/hooks/useDeviceOrientation.test.ts
    - src/components/CameraView.orientation.test.tsx
    - src/test/deviceOrientationMocks.ts
  key_links:
    - App handleEnableLiveCamera chains startCamera then requestOrientationPermission when !cameraError
    - CameraView orientation banner mirrors camera error banner layout
    - orientationDenied prop derived from orientationPermission === denied in App
  prohibitions:
    - Must not request orientation permission on app mount
    - Must not auto-retry orientation permission in loops (D-09)
    - Must not show simulate toggle when orientation permission denied on mobile (D-13)
---

# Plan 03-02: iOS Orientation Permission & Level Gauge Hardening

**As a** retail field rep holding my phone to re-shoot a shelf,
**I want to** grant motion permission when I enable the live camera and see a level gauge that snaps green when level,
**so that** I can align the camera before capture on iOS and other devices.

<objective>
Implement CAM-07 iOS orientation permission flow and harden CAM-03 level gauge behavior: lazy deviceorientation listener, App-level camera-then-orientation orchestration, inline denied banner with retry, simulate-toggle guard.

Purpose: Close the primary Phase 3 gap (orientation permission) while preserving existing level snap/haptic logic.
Output: Refactored useDeviceOrientation, App orchestration, orientation banner UI, unit/component tests.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-guided-capture-quality/03-CONTEXT.md
@.planning/phases/03-guided-capture-quality/03-RESEARCH.md
@.planning/phases/03-guided-capture-quality/03-PATTERNS.md
@.planning/phases/03-guided-capture-quality/03-01-SUMMARY.md
@src/hooks/useDeviceOrientation.ts
@src/hooks/useCameraStream.ts
@src/App.tsx
@src/components/CameraView.tsx
@src/components/CameraView.error.test.tsx
@src/lib/constants.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>useDeviceOrientation iOS permission + lazy listener (D-06, D-07, D-11, D-12, D-13, CAM-03, CAM-07)</name>
  <files>src/hooks/useDeviceOrientation.ts, src/test/deviceOrientationMocks.ts, src/hooks/useDeviceOrientation.test.ts</files>
  <behavior>
    - requestPermission resolves granted → orientationPermission granted, deviceorientation events update tilt
    - requestPermission resolves denied → orientationPermission denied, no listener attached, tilt stays 0
    - requestPermission API absent → orientationPermission unsupported, listener attaches immediately (non-iOS)
    - Gamma within ±1.5° sets isLevel true; outside sets false
    - setSimulatedTilt uses 800ms haptic debounce; real sensor path uses 1200ms
    - After 3s without deviceorientation events, hasSensor false (existing timeout pattern)
  </behavior>
  <action>
    Refactor src/hooks/useDeviceOrientation.ts per RESEARCH Pattern 1. Remove unconditional deviceorientation listener on mount. Add orientationPermission state typed granted | denied | prompt | unsupported. Export requestOrientationPermission async function.

    Feature-detect DeviceOrientationEvent.requestPermission. On non-iOS (API absent), set unsupported/granted path and attach listener immediately. On iOS, stay in prompt until requestOrientationPermission called; attach listener only on granted. Wrap requestPermission in try/catch — set denied on rejection; never throw to caller (D-10 independence).

    Retain existing handleOrientation gamma clamp ±30, level threshold ±1.5°, mint snap via isLevel, vibrate 40ms, debounce 1200ms real / 800ms simulate (D-11, D-12). Keep setSimulatedTilt export.

    Preserve 3s no-event timeout setting hasSensor false for desktop QA (D-13). Do not attach listener before permission grant on iOS.

    Create src/test/deviceOrientationMocks.ts with installRequestPermission and emitDeviceOrientation helpers per RESEARCH.md. Create src/hooks/useDeviceOrientation.test.ts using renderHook + act; mock requestPermission in beforeEach.
  </action>
  <verify>
    <automated>bun run test -- src/hooks/useDeviceOrientation.test.ts</automated>
    <fails_when>orientation permission unit tests fail or file missing</fails_when>
    <automated>grep -c 'requestOrientationPermission' src/hooks/useDeviceOrientation.ts | grep -qv '^0$'</automated>
    <fails_when>hook missing requestOrientationPermission export</fails_when>
    <automated>grep -c 'orientationPermission' src/hooks/useDeviceOrientation.ts | grep -qv '^0$'</automated>
    <fails_when>hook missing orientationPermission state</fails_when>
  </verify>
  <done>Permission flow unit-tested; listener lazy on iOS; level snap/haptic behavior retained.</done>
</task>

<task type="auto">
  <name>App camera-then-orientation orchestration + orientation banner (D-06, D-08, D-09, D-10, D-13, D-14, CAM-01, CAM-07)</name>
  <files>src/App.tsx, src/components/CameraView.tsx, src/lib/constants.ts</files>
  <action>
    Update App.tsx: destructure orientationPermission, requestOrientationPermission, hasSensor from useDeviceOrientation. Replace direct toggleDemoMode pass-through with handleEnableLiveCamera wrapper per RESEARCH Pattern 2: when isUsingDemoFeed, await startCamera(); if !cameraError, await requestOrientationPermission(). When leaving live mode, keep existing toggleDemoMode stop path unchanged (D-15 demo-first retained).

    Pass to CameraView: orientationDenied={orientationPermission === 'denied'}, onRetryOrientation={requestOrientationPermission}, onDismissOrientationError callback to clear dismissed state if needed, hasSensor={hasSensor}. Keep camera error props independent (D-10).

    Extend CameraViewProps with orientationDenied?, onRetryOrientation?, onDismissOrientationError?, hasSensor?. Add inline orientation-denied banner stacked below quotaError and cameraError blocks — copy camera error banner structure (role=alert, AlertCircle, retry button, dismiss X) per D-08. Use new I18N keys in constants.ts for cn and en: orientationPermissionDenied, orientationErrorIosGuide (Settings → Safari → Motion & Orientation), retryOrientation per Claude discretion.

    Update simulate toggle visibility: show onSimulateTiltToggle pill only when onSimulateTiltToggle && !orientationDenied && !hasSensor (D-13). Level crosshair block stays always rendered (D-14); when orientationDenied, crosshair uses gray/frozen styling at tilt 0.

    Do not request orientation on mount. Retry button calls requestOrientationPermission once per tap — no automatic re-prompt loops (D-09).
  </action>
  <verify>
    <automated>grep -c 'requestOrientationPermission' src/App.tsx | grep -qv '^0$'</automated>
    <fails_when>App does not chain orientation request after camera enable</fails_when>
    <automated>grep -c 'orientationPermissionDenied' src/lib/constants.ts | grep -qv '^0$'</automated>
    <fails_when>orientation I18N keys missing</fails_when>
    <automated>grep -c 'orientationDenied' src/components/CameraView.tsx | grep -qv '^0$'</automated>
    <fails_when>CameraView missing orientation denied banner wiring</fails_when>
  </verify>
  <done>Live camera toggle chains orientation request; denied banner with retry renders; simulate toggle hidden when denied; level crosshair always visible.</done>
</task>

<task type="auto" tdd="true">
  <name>Orientation banner component tests (D-18, CAM-07)</name>
  <files>src/components/CameraView.orientation.test.tsx</files>
  <behavior>
    - orientationDenied true → banner shows orientationPermissionDenied title and iOS guide text
    - onRetryOrientation called when retry button clicked
    - onDismissOrientationError called when dismiss clicked
    - orientationDenied true → simulate toggle not rendered even when onSimulateTiltToggle provided
  </behavior>
  <action>
    Create src/components/CameraView.orientation.test.tsx following CameraView.error.test.tsx baseProps pattern. Extend baseProps with hasPersistedBaseline false, orientationDenied, onRetryOrientation, onDismissOrientationError.

    Assert cn copy for orientationPermissionDenied and Safari guide substring. Test retry and dismiss handlers mirror error.test.tsx lines 38-61 pattern.

    Assert simulate pill absent when orientationDenied true and onSimulateTiltToggle provided.
  </action>
  <verify>
    <automated>bun run test -- src/components/CameraView.orientation.test.tsx</automated>
    <fails_when>orientation banner tests fail or file missing</fails_when>
    <automated>bun run test -- src/hooks/useDeviceOrientation.test.ts src/components/CameraView.orientation.test.tsx</automated>
    <fails_when>combined orientation test suite fails</fails_when>
  </verify>
  <done>Orientation denied banner and simulate-toggle guard covered by component tests; hook tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User gesture → permission APIs | Transient activation required for iOS orientation prompt |
| DeviceOrientationEvent → tilt state | Sensor data crosses into UI level gauge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Information Disclosure | Orientation on non-gesture path | high | mitigate | requestOrientationPermission only from handleEnableLiveCamera click chain (D-06) |
| T-03-05 | Information Disclosure | Over-permissioning absolute orientation | low | mitigate | Use gamma-only level gauge; do not request absolute orientation |
| T-03-06 | Denial of Service | Auto-retry permission loops | medium | mitigate | Retry only on explicit button tap (D-09) |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in Phase 3; RESEARCH Package Legitimacy Audit empty |

</threat_model>

<verification>
bun run test -- src/hooks/useDeviceOrientation.test.ts src/components/CameraView.orientation.test.tsx
grep -c 'requestOrientationPermission' src/App.tsx | grep -qv '^0$'
</verification>

<success_criteria>
Orientation permission requested after successful camera enable; denied banner with retry; level gauge crosshair always visible; unit and component tests green.
</success_criteria>

<output>
Create `.planning/phases/03-guided-capture-quality/03-02-SUMMARY.md` when done
</output>
