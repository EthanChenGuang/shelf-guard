---
phase: 03-guided-capture-quality
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/App.shelfIsolation.integration.test.tsx
autonomous: false
requirements:
  - CAM-01
  - CAM-02
  - CAM-03
  - CAM-07
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Shelf switch updates ghost img src to new shelf baseline without stale blob URL (D-03, D-19)
    - Full test suite, lint, and build pass for Phase 3
    - iOS device confirms orientation permission prompt on live-camera gesture and level gauge active after grant (CAM-07 manual)
  artifacts:
    - src/App.shelfIsolation.integration.test.tsx
  key_links:
    - Integration test mocks live camera isUsingDemoFeed false with persisted baselines on multiple shelves
    - Ghost overlay src attribute changes on shelf switch when hasPersistedBaseline true
  prohibitions:
    - Must not skip iOS manual verification for CAM-07 success criterion
---

# Plan 03-03: Shelf Ghost Integration & Phase Verification Gate

**As a** retail field rep switching between calibrated shelves,
**I want to** see the correct ghost overlay for each shelf without stale images,
**so that** multi-shelf alignment stays accurate before vision analysis.

<objective>
Extend Phase 2 shelf isolation integration test for ghost URL swap on shelf switch (D-19). Run Phase 3 verification gate: full automated suite plus iOS device check for CAM-07.

Purpose: Prove cross-shelf ghost hygiene and close Phase 3 with ROADMAP success criteria verified.
Output: Extended integration test, phase gate green, iOS manual verification recorded.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-guided-capture-quality/03-CONTEXT.md
@.planning/phases/03-guided-capture-quality/03-RESEARCH.md
@.planning/phases/03-guided-capture-quality/03-PATTERNS.md
@.planning/phases/03-guided-capture-quality/03-01-SUMMARY.md
@.planning/phases/03-guided-capture-quality/03-02-SUMMARY.md
@src/App.shelfIsolation.integration.test.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Shelf switch ghost source integration test (D-03, D-19, CAM-02)</name>
  <files>src/App.shelfIsolation.integration.test.tsx</files>
  <behavior>
    - With live camera mock (isUsingDemoFeed false) and persisted baselines on shelf 0 and shelf 1, ghost overlay img src differs after shelf switch
    - Switching back to shelf 0 restores shelf 0 ghost src — no stale shelf 1 blob URL
    - data-baseline-id on camera-view updates per shelf switch (existing pattern retained)
  </behavior>
  <action>
    Extend src/App.shelfIsolation.integration.test.tsx per PATTERNS.md. Update useDeviceOrientation mock to include orientationPermission granted and requestOrientationPermission vi.fn().

    Update useCameraStream mock to expose isUsingDemoFeed false (live camera path) so ghost overlay renders when hasPersistedBaseline true.

    Seed shelf 0 and shelf 1 with distinct baselines via saveBaseline + makeCalibration helpers (existing pattern lines 138-159). Render App; wait for shelf 0 baseline id. Assert getByTestId ghost-overlay or getByAltText Baseline Ghost Overlay img src contains shelf 0 blob URL pattern.

    Click ShelfSelector label 2 (index 1); waitFor baseline id shelf 1. Assert ghost img src changed from shelf 0 src. Switch back to label 1 (index 0); assert ghost src matches original shelf 0 src — proves no stale URL leak (D-19).

    Reuse fake-indexeddb, stubCompressionGlobals, waitForBaselineId helper. Do not mock shelfStorage.
  </action>
  <verify>
    <automated>bun run test -- src/App.shelfIsolation.integration.test.tsx</automated>
    <fails_when>ghost shelf switch integration test fails</fails_when>
    <automated>grep -c 'Baseline Ghost Overlay\|ghost-overlay' src/App.shelfIsolation.integration.test.tsx | grep -qv '^0$'</automated>
    <fails_when>integration test missing ghost assertion</fails_when>
  </verify>
  <done>Integration test proves ghost img src updates on shelf switch without stale blob URLs.</done>
</task>

<task type="auto">
  <name>Phase 3 automated verification gate</name>
  <files>src/App.shelfIsolation.integration.test.tsx</files>
  <action>
    Run full Phase 3 targeted test bundle then project-wide lint, build, and test suite. Confirm all new test files exist and pass: useDeviceOrientation.test.ts, CameraView.ghost.test.tsx, CameraView.orientation.test.tsx, extended App.shelfIsolation.integration.test.tsx.

    Verify ROADMAP Phase 3 automated success criteria: demo/live feed (CAM-01), ghost visibility (CAM-02), level hook tests (CAM-03), orientation permission unit/component tests (CAM-07).
  </action>
  <verify>
    <automated>bun run test -- src/hooks/useDeviceOrientation.test.ts src/components/CameraView.ghost.test.tsx src/components/CameraView.orientation.test.tsx src/App.shelfIsolation.integration.test.tsx</automated>
    <fails_when>Phase 3 targeted tests fail</fails_when>
    <automated>bun run lint && bun run build && bun run test</automated>
    <fails_when>lint, build, or full test suite fails</fails_when>
    <human-check>
      On iOS Safari or installed PWA: open app (demo feed default), tap demo/camera toggle to enable live camera. Confirm native Motion & Orientation permission prompt appears on same gesture. Grant permission — level crosshair responds to phone roll within ±1.5° snap. Deny permission — inline orientation banner with Retry appears; camera feed still works. Re-test Retry re-prompts once per tap.
    </human-check>
  </verify>
  <done>Full suite green; all Phase 3 automated tests pass; iOS manual CAM-07 check documented in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>iOS orientation permission device verification (CAM-07)</name>
  <what-built>Phase 3 guided capture: ghost visibility rules, iOS orientation permission on live-camera gesture, orientation denied banner, level gauge with ±1.5° snap.</what-built>
  <how-to-verify>
    1. Deploy preview or run dev server accessible from iOS device over HTTPS (or localhost if testing in Safari on Mac with sensor).
    2. Open ShelfGuard in iOS Safari or installed PWA.
    3. Confirm demo feed shows on launch (no auto camera start).
    4. Tap demo/camera toggle — grant camera if prompted, then grant Motion & Orientation when prompted.
    5. Rotate phone — crosshair turns mint green and haptic fires within ±1.5° of level.
    6. Reset site permissions; repeat toggle — deny orientation. Confirm inline banner with Settings guide and Retry; camera feed still visible; ghost shows only on shelf with persisted baseline in live mode.
  </how-to-verify>
  <resume-signal>Type "approved" if CAM-07 verified on device, or describe issues for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → production iOS behavior | Automated tests cannot verify native permission dialog |
| Multi-shelf blob URLs | Integration test proves registry hygiene |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Information Disclosure | Stale ghost blob URL | medium | mitigate | Integration test asserts src swap on shelf change (D-19) |
| T-03-08 | Information Disclosure | iOS permission regression | high | accept | Manual device checkpoint; CI uses mocked permission (RESEARCH Environment Availability) |

</threat_model>

<verification>
bun run lint && bun run build && bun run test
bun run test -- src/App.shelfIsolation.integration.test.tsx
</verification>

<success_criteria>
Ghost URL isolation proven by integration test; full lint/build/test green; iOS device confirms orientation permission on live-camera gesture and level gauge after grant.
</success_criteria>

<output>
Create `.planning/phases/03-guided-capture-quality/03-03-SUMMARY.md` when done
</output>
