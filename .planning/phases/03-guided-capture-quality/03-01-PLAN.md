---
phase: 03-guided-capture-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/CameraView.tsx
  - src/components/CameraView.ghost.test.tsx
autonomous: true
requirements:
  - CAM-01
  - CAM-02
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Demo feed is default on launch; user opts into live camera via existing toggle (D-15, CAM-01)
    - Ghost overlay and right-edge slider render only when live camera is active AND shelf has persisted IndexedDB baseline (D-01, D-02, CAM-02)
    - Empty shelves show live/demo feed without ghost until user calibrates — no DEFAULT_CALIBRATION ghost (D-02)
    - Ghost source is baseline.imageDataUrl from active shelf; updates on shelf switch via loadShelfData (D-03)
    - Global ghost opacity defaults to 45% in App state; not persisted to IndexedDB (D-04)
    - Ghost keeps mix-blend-screen compositing (D-05)
  artifacts:
    - src/components/CameraView.ghost.test.tsx
  key_links:
    - loadShelfData sets hasPersistedBaseline from loadBaselineRaw !== null
    - CameraView showGhost = !isUsingDemoFeed && hasPersistedBaseline
    - Ghost slider wrapped in same showGhost condition as overlay div
  prohibitions:
    - Must not ghost-overlay DEFAULT_CALIBRATION CDN on empty shelves
    - Must not persist ghost opacity to IndexedDB (deferred)
---

# Plan 03-01: Tracer — Ghost Overlay Visibility End-to-End

**As a** retail field rep re-inspecting a calibrated shelf,
**I want to** see the baseline ghost overlay only when using the live camera on a shelf that has a saved baseline,
**so that** demo mode stays clear and empty shelves do not show a misleading alignment target.

<objective>
Wire the production-quality tracer slice for CAM-02 ghost visibility: App tracks hasPersistedBaseline separately from view baseline, CameraView gates ghost overlay and slider on live feed + persisted baseline, demo-first default retained (CAM-01).

Purpose: Fix double baseline render in demo mode and ghost on empty shelves before iOS orientation work in 03-02.
Output: hasPersistedBaseline wiring, conditional ghost/slider, ghost visibility component tests.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-guided-capture-quality/03-CONTEXT.md
@.planning/phases/03-guided-capture-quality/03-RESEARCH.md
@.planning/phases/03-guided-capture-quality/03-PATTERNS.md
@.planning/phases/02-multi-shelf-data-layer/02-02-SUMMARY.md
@src/App.tsx
@src/components/CameraView.tsx
@src/components/CameraView.error.test.tsx
@src/lib/constants.ts
</context>

<tasks>

<task type="tracer">
  <name>End-to-end ghost overlay — live camera + persisted baseline only (D-01, D-02, D-03, D-04, D-05, CAM-02)</name>
  <files>src/App.tsx, src/components/CameraView.tsx</files>
  <read_first>
    src/App.tsx
    src/components/CameraView.tsx
    .planning/phases/03-guided-capture-quality/03-CONTEXT.md
    .planning/phases/03-guided-capture-quality/03-PATTERNS.md
  </read_first>
  <action>
    Add hasPersistedBaseline boolean state in App.tsx (default false). Inside loadShelfData, after loadBaselineRaw(shelfId): setHasPersistedBaseline(persisted !== null) per D-02. Keep existing DEFAULT_CALIBRATION fallback for demo feed display when persisted is null — do not use DEFAULT_CALIBRATION id baseline-default as ghost eligibility.

    Extend CameraViewProps with hasPersistedBaseline?: boolean (default false). Derive showGhost = !isUsingDemoFeed && hasPersistedBaseline && baseline. Wrap ghost overlay block (currently unconditional at baseline &&) and right-edge vertical slider (lines ~320-357) in showGhost guard per D-01. Add data-testid="ghost-overlay" on ghost div per PATTERNS.md.

    Retain ghostOpacity global App state at default 45 (D-04). Pass hasPersistedBaseline={hasPersistedBaseline} to CameraView. Do not add IndexedDB persistence for opacity.

    Preserve demo-first isUsingDemoFeed default from useCameraStream (D-15, CAM-01). Demo feed continues showing baseline.imageDataUrl only — no second ghost layer in demo mode.

    On shelf switch, existing handleShelfChange + loadShelfData already swaps baseline.imageDataUrl via objectUrlRegistry — ghost img src follows automatically (D-03). No cross-shelf bleed changes needed beyond hasPersistedBaseline update.
  </action>
  <verify>
    <automated>grep -c 'hasPersistedBaseline' src/App.tsx src/components/CameraView.tsx | grep -qv '^0$'</automated>
    <fails_when>hasPersistedBaseline not wired in App or CameraView</fails_when>
    <automated>grep -c 'data-testid="ghost-overlay"' src/components/CameraView.tsx | grep -qv '^0$'</automated>
    <fails_when>ghost overlay missing test id for verification</fails_when>
    <automated>grep -c 'isUsingDemoFeed && hasPersistedBaseline' src/components/CameraView.tsx | grep -qv '^0$'</automated>
    <fails_when>ghost visibility gate missing both live-feed and persisted-baseline conditions</fails_when>
  </verify>
  <done>Live camera + persisted baseline shows ghost overlay and slider; demo mode and empty shelves show feed without ghost layer; ghostOpacity remains global 45% default.</done>
</task>

<task type="auto" tdd="true">
  <name>Ghost visibility matrix component tests (D-18, CAM-02)</name>
  <files>src/components/CameraView.ghost.test.tsx</files>
  <behavior>
    - Demo feed + hasPersistedBaseline true → ghost overlay absent, slider aria-label absent
    - Live feed + hasPersistedBaseline false → ghost overlay absent, slider absent
    - Live feed + hasPersistedBaseline true → ghost overlay present (data-testid ghost-overlay), slider present (aria-label 幽灵图透光率)
    - Ghost img uses baseline.imageDataUrl alt Baseline Ghost Overlay
  </behavior>
  <action>
    Create src/components/CameraView.ghost.test.tsx following CameraView.error.test.tsx baseProps fixture pattern. Import DEFAULT_CALIBRATION from constants. Use render/screen from @testing-library/react.

    Cover the four visibility cases above. Use queryByTestId('ghost-overlay') and queryByLabelText with t.ghostOpacity cn string 幽灵图透光率 per RESEARCH.md. Assert getByAltText('Baseline Ghost Overlay') only in live+persisted case.

    Do not test orientation banner here — that belongs in 03-02.
  </action>
  <verify>
    <automated>bun run test -- src/components/CameraView.ghost.test.tsx</automated>
    <fails_when>ghost visibility tests fail or file missing</fails_when>
  </verify>
  <done>All four ghost visibility matrix cases pass; demo double-render regression locked by test.</done>
</task>

<task type="auto">
  <name>Confirm loadShelfData baseline eligibility wiring (D-02, D-04)</name>
  <files>src/App.tsx</files>
  <action>
    Verify loadShelfData sets hasPersistedBaseline synchronously with baseline state update — both set in same async function before return. Ensure handleShelfChange path calls loadShelfData so shelf switch updates hasPersistedBaseline.

    Confirm ghostOpacity useState(45) unchanged and not written to shelfStorage or storage.ts. No new IndexedDB keys for opacity.
  </action>
  <verify>
    <automated>grep -c 'setHasPersistedBaseline' src/App.tsx | grep -qv '^0$'</automated>
    <fails_when>loadShelfData does not update hasPersistedBaseline</fails_when>
    <automated>grep -c 'ghostOpacity' src/lib/shelfStorage.ts src/lib/storage.ts | grep -qx '0'</automated>
    <fails_when>ghost opacity incorrectly persisted to storage layer</fails_when>
    <automated>bun run test -- src/components/CameraView.ghost.test.tsx</automated>
    <fails_when>regression in ghost tests after wiring check</fails_when>
  </verify>
  <done>hasPersistedBaseline tracks loadBaselineRaw result on init and shelf switch; ghost opacity stays ephemeral App state at 45%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB baseline → ghost img src | Persisted blob URLs must not leak across shelf contexts |
| Ghost opacity slider input | User-controlled 0–100 range crosses into CSS opacity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Ghost on empty shelf | medium | mitigate | hasPersistedBaseline gate; exclude DEFAULT_CALIBRATION id baseline-default (D-02) |
| T-03-02 | Tampering | ghostOpacity slider | low | mitigate | Clamp input 0–100 via range min/max; existing Number() cast on change |
| T-03-03 | Information Disclosure | Blob URL cross-shelf | medium | mitigate | Retain registry.revokeAll on shelf switch from Phase 2 (D-03) |

</threat_model>

<verification>
bun run test -- src/components/CameraView.ghost.test.tsx
grep -c 'hasPersistedBaseline' src/App.tsx | grep -qv '^0$'
</verification>

<success_criteria>
Ghost hidden in demo mode and on empty shelves; visible on live camera with persisted baseline; demo-first default retained; ghost visibility tests green.
</success_criteria>

<output>
Create `.planning/phases/03-guided-capture-quality/03-01-SUMMARY.md` when done
</output>
