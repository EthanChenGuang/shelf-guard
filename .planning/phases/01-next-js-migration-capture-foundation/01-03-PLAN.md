---
phase: 01-next-js-migration-capture-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - src/App.tsx
  - src/hooks/useCameraStream.ts
  - src/components/CameraView.tsx
  - src/components/ScanningAnimationOverlay.tsx
  - src/lib/constants.ts
autonomous: true
requirements:
  - STAB-01
  - STAB-02
  - STAB-03
  - STAB-04
estimate:
  tokens: 60000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Second shutter tap ignored while appMode is SCANNING_ANIM or PROCESSING per D-16/STAB-01
    - Shutter button shows disabled styling and aria-label shutterLocked when capture locked per UI-SPEC
    - Torch button hidden when hasTorch is false or isUsingDemoFeed is true per D-17/STAB-02
    - isTorchOn never toggles without successful torch constraint apply per D-17
    - When analysis exceeds 800ms SCANNING_ANIM, appMode transitions to PROCESSING with visible overlay per D-15/STAB-03
    - ScanningAnimationOverlay variant processing shows t.processing copy and Loader2 spinner without scan beam per UI-SPEC
    - Custom baseline upload sets imageDimensions from Image naturalWidth/naturalHeight per D-18/STAB-04
    - FSM patches remain in App.tsx without useAuditFlow extraction per D-14
  artifacts:
    - hasTorch export from useCameraStream
    - captureLockRef in App.tsx
    - ScanningAnimationOverlay variant prop
  key_links:
    - App.tsx passes isShutterLocked to CameraView from appMode and captureLockRef
    - handleShutterClick decouples 800ms animation timer from analysis Promise
    - handleUploadCustomBaseline awaits Image onload for dimensions before saveBaseline
  prohibitions:
    - Must not extract useAuditFlow or decompose App.tsx FSM into new hooks per D-14
---

# Plan 01-03: FSM Stability & Capture Guards

**As a** retail field rep,
**I want** the shutter, torch, processing state, and baseline upload to behave reliably,
**so that** capture never double-fires, UI never lies about hardware, and uploads carry correct dimensions.

<objective>
Patch central App.tsx FSM and useCameraStream for STAB-01 through STAB-04 without hook extraction.

Purpose: Fix known race conditions and silent UI lies before camera error UX plan.
Output: Shutter lock, PROCESSING overlay, honest torch, baseline dimension fix.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-next-js-migration-capture-foundation/01-CONTEXT.md
@.planning/phases/01-next-js-migration-capture-foundation/01-UI-SPEC.md
@.planning/phases/01-next-js-migration-capture-foundation/01-PATTERNS.md
@src/App.tsx
@src/hooks/useCameraStream.ts
@src/components/CameraView.tsx
@src/components/ScanningAnimationOverlay.tsx
@src/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Shutter double-tap guard and PROCESSING FSM (STAB-01, STAB-03)</name>
  <files>src/App.tsx, src/components/ScanningAnimationOverlay.tsx, src/components/CameraView.tsx, src/lib/constants.ts, src/App.capture.test.ts, src/App.processing.test.ts</files>
  <read_first>
    src/App.tsx
    src/types.ts
    src/components/ScanningAnimationOverlay.tsx
    src/lib/constants.ts
    .planning/phases/01-next-js-migration-capture-foundation/01-UI-SPEC.md
    .planning/phases/01-next-js-migration-capture-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test STAB-01: handleShutterClick returns early without state change when captureLockRef is already true
    - Test STAB-03: when analyzeShelfCapture is delayed beyond 800ms, appMode becomes PROCESSING before RESULT_INSPECT
  </behavior>
  <action>
    Add captureLockRef useRef in App.tsx. At start of handleShutterClick, if appMode is SCANNING_ANIM or PROCESSING or captureLockRef.current is true, return immediately per D-16. Set captureLockRef true before capture; clear when transitioning to RESULT_INSPECT or on error.

    Refactor handleShutterClick per RESEARCH Pattern 3: start analyzeShelfCapture Promise immediately; set SCANNING_ANIM; after 800ms timeout check if analysis done — if not, setAppMode PROCESSING; when analysis completes apply results then set RESULT_INSPECT and clear lock. Store timeout id in ref and clear on unmount if needed.

    Extend ScanningAnimationOverlay with optional variant prop scanning | processing default scanning per UI-SPEC section 4. Processing variant shows static pulse overlay, Loader2 icon, t.processing string — no scan beam animation. Add processing and shutterLocked keys to I18N cn/en in constants.ts.

    Render overlay for both SCANNING_ANIM and PROCESSING with variant derived from appMode. Pass isShutterLocked prop to CameraView true when appMode is SCANNING_ANIM or PROCESSING; apply disabled opacity-50 cursor-not-allowed pointer-events-none and aria-disabled aria-label t.shutterLocked on shutter button per UI-SPEC section 2.

    For STAB-03 testability, export a test helper or use vi.mock on analyzeShelfCapture with delayed Promise in App.processing.test.ts and App.capture.test.ts.
  </action>
  <acceptance_criteria>
    - captureLockRef guards handleShutterClick against re-entry during scan
    - PROCESSING appMode renders ScanningAnimationOverlay with processing variant
    - CameraView shutter disabled when isShutterLocked true
    - handleShutterClick still calls captureFrame(baseline.imageDataUrl) — CAM-09 wiring from 01-02 preserved after FSM refactor
    - App.capture.test.ts and App.processing.test.ts pass
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'captureFrame(baseline.imageDataUrl)' src/App.tsx | grep -qv '^0$'</automated>
    <fails_when>CAM-09 demo capture wiring removed during FSM refactor</fails_when>
    <automated>bun run test -- src/App.capture.test.ts src/App.processing.test.ts</automated>
    <fails_when>non-zero exit or tests still skipped</fails_when>
    <automated>grep -c "PROCESSING" src/App.tsx | grep -qv '^0$'</automated>
    <fails_when>App.tsx does not reference PROCESSING mode in render or handler</fails_when>
  </verify>
  <done>STAB-01 and STAB-03 implemented with automated tests; PROCESSING UI visible when analysis slow.</done>
</task>

<task type="auto" tdd="true">
  <name>Torch capability honesty (STAB-02)</name>
  <files>src/hooks/useCameraStream.ts, src/components/CameraView.tsx, src/hooks/useCameraStream.torch.test.ts</files>
  <read_first>
    src/hooks/useCameraStream.ts
    src/components/CameraView.tsx
    .planning/phases/01-next-js-migration-capture-foundation/01-PATTERNS.md
  </read_first>
  <behavior>
    - Test: hasTorch is false when getCapabilities lacks torch key
    - Test: toggleTorch does not call setIsTorchOn when torch unsupported
  </behavior>
  <action>
    Add hasTorch state in useCameraStream, set true only after stream attaches and video track getCapabilities includes torch key. Remove fallthrough branches that toggle isTorchOn when stream is null or torch missing (lines 56-57 and 76 pattern per PATTERNS). On constraint apply failure, log warning and leave isTorchOn unchanged.

    Export hasTorch from hook return object. In CameraView, render torch button only when hasTorch is true AND isUsingDemoFeed is false per UI-SPEC section 3 — hide entirely, no disabled placeholder.

    Implement tests in useCameraStream.torch.test.ts with mocked MediaStream and getCapabilities.
  </action>
  <acceptance_criteria>
    - hasTorch exported from useCameraStream
    - CameraView conditionally renders torch based on hasTorch
    - useCameraStream.torch.test.ts passes
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/hooks/useCameraStream.torch.test.ts</automated>
    <fails_when>non-zero exit or skipped tests</fails_when>
    <automated>grep -c 'hasTorch' src/hooks/useCameraStream.ts src/components/CameraView.tsx | grep -qv '^0$'</automated>
    <fails_when>hasTorch not wired in hook or view</fails_when>
  </verify>
  <done>STAB-02 torch UI matches hardware capability.</done>
</task>

<task type="auto" tdd="true">
  <name>Baseline upload imageDimensions fix (STAB-04)</name>
  <files>src/App.tsx, src/App.baseline.test.ts</files>
  <read_first>
    src/App.tsx
    src/types.ts
    src/lib/constants.ts
    .planning/phases/01-next-js-migration-capture-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test: after handleUploadCustomBaseline with mocked FileReader result, saved calibration includes imageDimensions matching mocked Image naturalWidth/Height
  </behavior>
  <action>
    In handleUploadCustomBaseline reader.onload handler per RESEARCH Pattern STAB-04: after dataUrl obtained, await new Promise resolving Image onload with naturalWidth and naturalHeight; set imageDimensions on new ShelfCalibration before setBaseline and saveBaseline. Keep existing flow to ROI_CONFIG mode.

    Implement App.baseline.test.ts testing dimension assignment logic (extract pure async helper loadImageDimensions(dataUrl) if needed for unit test isolation).
  </action>
  <acceptance_criteria>
    - handleUploadCustomBaseline sets imageDimensions from loaded image natural size
    - App.baseline.test.ts passes
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/App.baseline.test.ts</automated>
    <fails_when>non-zero exit or skipped tests</fails_when>
  </verify>
  <done>STAB-04 custom baseline upload carries correct imageDimensions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser MediaDevices | Torch constraints applied to video track |
| User file upload | Custom baseline image read via FileReader |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Denial of Service | Baseline upload | medium | mitigate | Validate image loads before save; reject empty dataUrl in upload handler |
| T-01-03 | Tampering | FSM state | low | accept | captureLockRef prevents duplicate analysis runs |
</threat_model>

<verification>
bun run lint && bun run test for App.* and useCameraStream.torch tests.
</verification>

<success_criteria>
All STAB requirements satisfied with tests; FSM remains monolithic in App.tsx per D-14.
</success_criteria>

## Artifacts this phase produces

- captureLockRef — shutter re-entry guard in App.tsx
- hasTorch — boolean export from useCameraStream
- isShutterLocked — CameraView prop for shutter disabled state
- ScanningAnimationOverlay variant prop — scanning | processing overlay modes
- I18N keys processing, shutterLocked — constants.ts bilingual strings

<output>
Create `.planning/phases/01-next-js-migration-capture-foundation/01-03-SUMMARY.md` when done
</output>
