---
phase: 01-next-js-migration-capture-foundation
plan: 06
type: execute
wave: 4
depends_on:
  - 01-05
gap_closure: true
gap_ids:
  - G-01-4
autonomous: true
files_modified:
  - src/components/OfflineIndicator.tsx
  - src/components/OfflineIndicator.test.tsx
requirements:
  - PWA-03
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Offline indicator pill bottom edge clears #shutter-trigger top (532px) at 320×640 viewport
    - Offline indicator text does not clip at 320px viewport width
    - G-01-4 layout test asserts clearance against measured shutter top, not shutter band bottom
  artifacts:
    - src/components/OfflineIndicator.tsx with bottom-28 or bottom-[6.75rem] fixed offset
    - src/components/OfflineIndicator.test.tsx with non-circular shutter-top clearance assertion
  key_links:
    - OfflineIndicator fixed bottom offset ≥108px clears Playwright-measured shutter top at 532px on 640px viewport
---

# Plan 01-06 — Close G-01-4 offline pill overlap (second pass)

<objective>
Close UAT gap G-01-4: offline pill still overlaps #shutter-trigger by 12px at 320×640 after plan 01-05 bottom-24 fix.

Purpose: Plan 01-05 cleared the shutter band bottom (608px) in unit-test math but Playwright measured pill bottom 544px overlapping shutter top 532px. This plan raises the offset and fixes the regression test to match real layout geometry.
Output: OfflineIndicator at bottom-28+ with layout test asserting pillRect.bottom ≤ shutterTop (532).
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-next-js-migration-capture-foundation/01-UAT.md
@.planning/phases/01-next-js-migration-capture-foundation/01-05-SUMMARY.md
@.planning/phases/01-next-js-migration-capture-foundation/01-REVIEW.md
@src/components/OfflineIndicator.tsx
@src/components/OfflineIndicator.test.tsx
@src/components/CameraView.tsx
</context>

<tasks>

<task type="tracer">
  <name>Raise offline pill to bottom-28 — clears shutter top at 320×640</name>
  <files>src/components/OfflineIndicator.tsx</files>
  <read_first>
    src/components/OfflineIndicator.tsx
    .planning/phases/01-next-js-migration-capture-foundation/01-UAT.md
  </read_first>
  <action>
    Gap G-01-4: Playwright at 320×640 measured pill with bottom-24 at top 514 / bottom 544 overlapping #shutter-trigger top 532 by 12px. Shutter band bottom is 608px — clearing band bottom alone is insufficient.

    Change the fixed positioning class on the offline pill container from bottom-24 to bottom-28 (112px / 7rem). Prefer bottom-28 over bottom-[6.75rem] (108px) because 112px yields pill bottom 528px — 4px clearance below shutter top 532px. bottom-[6.75rem] would sit exactly flush at 532px with zero margin.

    Keep all other classes unchanged: left-4, z-50, rounded-full, amber styling, WifiOff icon, I18N offlineMode span.

    Do not revert to bottom-3 or bottom-24. Do not change CameraView shutter layout — only raise the pill.
  </action>
  <verify>
    <automated>grep -v '^#' src/components/OfflineIndicator.tsx | grep -c 'bottom-28' | grep -qv '^0$'</automated>
    <fails_when>bottom-28 class absent from OfflineIndicator</fails_when>
    <automated>grep -v '^#' src/components/OfflineIndicator.tsx | grep -c 'bottom-24' | grep -q '^0$'</automated>
    <fails_when>stale bottom-24 class still present</fails_when>
  </verify>
  <done>OfflineIndicator renders with bottom-28 fixed offset; bottom-24 removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Fix G-01-4 layout test — assert shutter top 532 clearance</name>
  <files>src/components/OfflineIndicator.test.tsx</files>
  <read_first>
    src/components/OfflineIndicator.test.tsx
    .planning/phases/01-next-js-migration-capture-foundation/01-REVIEW.md
    .planning/phases/01-next-js-migration-capture-foundation/01-UAT.md
  </read_first>
  <behavior>
    - Test: at 320×640 with navigator offline, pill className contains bottom-28 (not bottom-24)
    - Test: computed pill bottom edge (viewportHeight − bottom28Px) is less than or equal to Playwright-measured shutterTop 532
    - Test: computed pill bottom is strictly less than pre-fix bottom-24 position (544) proving regression guard
    - Test does NOT mock getBoundingClientRect with values derived from the same constants used in assertions (non-circular)
  </behavior>
  <action>
    Gap G-01-4 root cause in test: prior test asserted pillRect.bottom ≤ shutterBandBottom (608) using a mocked getBoundingClientRect fed from bottom24Px=96 — circular proof that never validated clearance against shutter top 532.

    Rewrite the positions pill above shutter band at 320px viewport (G-01-4) test:

    1. Use Playwright-measured reference constants independent of component implementation: viewportHeight=640, viewportWidth=320, shutterTop=532 (measured #shutter-trigger top), bottom28Px=112 (Tailwind bottom-28 = 7rem).

    2. Assert pill.closest div className contains bottom-28 and does not contain bottom-24.

    3. Compute expectedPillBottom = viewportHeight - bottom28Px (= 528). Assert expectedPillBottom ≤ shutterTop. Assert expectedPillBottom &lt; 544 (prior bottom-24 pill bottom that failed UAT).

    4. Remove vi.spyOn(pill, getBoundingClientRect) mock entirely — jsdom does not compute fixed layout, so assert offset math against measured shutter geometry instead of a self-referential rect mock.

    5. Add a one-line comment citing UAT source: shutterTop 532 from Playwright 320×640 measurement in 01-UAT.md test 4.

    Keep existing online/null and I18N text tests unchanged.
  </action>
  <verify>
    <automated>bun run test -- src/components/OfflineIndicator.test.tsx</automated>
    <fails_when>non-zero exit or fewer than 3 passing tests</fails_when>
    <automated>grep -v '^#' src/components/OfflineIndicator.test.tsx | grep -c 'shutterTop' | grep -qv '^0$'</automated>
    <fails_when>shutterTop reference constant missing from test</fails_when>
    <automated>grep -v '^#' src/components/OfflineIndicator.test.tsx | grep -c 'getBoundingClientRect' | grep -q '^0$'</automated>
    <fails_when>circular getBoundingClientRect mock still present</fails_when>
  </verify>
  <done>G-01-4 layout test passes with shutter-top clearance math; no circular rect mock; all 3 OfflineIndicator tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI layout | Fixed-position pill vs shutter control — no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06-01 | Tampering | OfflineIndicator.tsx class | low | accept | CSS-only layout change; no auth or data boundary |
</threat_model>

<verification>
- `bun run test -- src/components/OfflineIndicator.test.tsx` — 3/3 pass
- Manual/UAT backstop: at 320×640 with offline event, pill bottom ≤ 532px (no overlap with #shutter-trigger)
- Re-run UAT test 4 via `/gsd-verify-work 01` after execution
</verification>

<success_criteria>
- G-01-4 truth satisfied: offline indicator does not overlap shutter control at 320px viewport
- Layout regression test guards against shutter top (532), not band bottom (608)
- No circular getBoundingClientRect mock in OfflineIndicator.test.tsx
</success_criteria>

<output>
Create `.planning/phases/01-next-js-migration-capture-foundation/01-06-SUMMARY.md` when done
</output>

<!-- Multi-Source Coverage Audit
SOURCE   | ID     | Feature/Requirement                              | Plan  | Status  | Notes
-------- | ------ | ------------------------------------------------ | ----- | ------- | -----
GOAL     | —      | Offline indicator usable without shutter overlap | 01-06 | COVERED | UAT test 4 truth
REQ      | PWA-03 | Offline indicator when disconnected            | 01-06 | COVERED |
RESEARCH | —      | bottom-28+ clears shutter top 532 at 320×640     | 01-06 | COVERED | From 01-UAT.md root_cause
CONTEXT  | G-01-4 | Pill clearance + non-circular layout test        | 01-06 | COVERED | gap_ids frontmatter
-->
