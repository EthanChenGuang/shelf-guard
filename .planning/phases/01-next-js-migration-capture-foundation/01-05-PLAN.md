---
phase: 01-next-js-migration-capture-foundation
plan: 05
type: execute
gap_closure: true
gap_ids: [G-01-4]
autonomous: true
files_modified:
  - src/components/OfflineIndicator.tsx
requirements:
  - PWA-03
---

# Plan 01-05 — Fix offline pill overlap at 320px (gap G-01-4)

## Goal

Offline indicator must not overlap the shutter control at 320px viewport width.

## Root cause

`OfflineIndicator` uses `fixed bottom-3 left-4`. On 320×640 the pill sits at y≈598–628 while the 76px shutter spans y≈532–608.

## Tasks

1. **Raise pill position** — Change `bottom-3` to `bottom-24` (or `bottom-[6.5rem]`) so the pill sits above the shutter row on narrow viewports. Keep `left-4` and existing styling.

2. **Add layout regression test** — Extend `OfflineIndicator.test.tsx` or add a small visual/layout test asserting pill bottom edge is above a mock shutter band at 320px (jsdom getBoundingClientRect with mocked layout if needed; optional Playwright held-out test).

3. **Re-run UAT test 4** — `/gsd-verify-work 01` after fix; confirm overlap resolved at 320×640.

## Verification

- `bun run test`
- Playwright/manual: 320px viewport, offline event, pill does not overlap `#shutter-trigger`
