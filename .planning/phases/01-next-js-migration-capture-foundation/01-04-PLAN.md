---
phase: 01-next-js-migration-capture-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-03
files_modified:
  - src/hooks/useCameraStream.ts
  - src/App.tsx
  - src/components/CameraView.tsx
  - src/lib/constants.ts
  - src/components/OfflineIndicator.tsx
autonomous: true
requirements:
  - CAM-08
  - PWA-03
  - PWA-01
estimate:
  tokens: 55000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - cameraError from useCameraStream renders inline banner on CameraView per D-07/D-09/CAM-08
    - Banner shows cameraPermissionDenied headline and cameraErrorIosGuide body from I18N per D-08
    - Banner includes Retry (startCamera) and Dismiss (clearCameraError) actions per UI-SPEC
    - Camera errors are never console-only — user sees banner while on demo feed after failure
    - OfflineIndicator receives lang prop and shows I18N offlineMode when navigator.onLine is false per PWA-03
    - OfflineIndicator returns null when online per UI-SPEC empty state
    - PWA installable with offline app shell after Vercel deploy per PWA-01
    - Demo feed remains default on first launch per D-10
    - { statement: offline pill at 320px viewport width does not clip text or overlap shutter, verification: backstop }
  artifacts:
    - clearCameraError or dismissCameraError from useCameraStream
    - CameraView camera error banner UI block
  key_links:
    - App.tsx passes cameraError, onRetryCamera, onDismissCameraError to CameraView
    - OfflineIndicator lang prop wired from App.tsx
  prohibitions:
    - Must not use full-screen modal or separate route for camera errors per D-07
    - Must not hardcode iOS guidance strings in CameraView — use I18N keys only per D-08
---

# Plan 01-04: Camera Error UX & Offline Indicator

**As a** retail field rep,
**I want** clear camera failure guidance and a visible offline state,
**so that** I know how to recover on iOS PWA and when the app is working offline.

<objective>
Wire cameraError to inline CameraView banner (CAM-08) and internationalize OfflineIndicator (PWA-03); finalize Phase 1 automated test coverage.

Purpose: Complete user-visible error and platform feedback for capture foundation.
Output: Error banner, I18N keys, passing component tests, PWA-01 ready for deploy verify.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-next-js-migration-capture-foundation/01-UI-SPEC.md
@.planning/phases/01-next-js-migration-capture-foundation/01-CONTEXT.md
@.planning/phases/01-next-js-migration-capture-foundation/01-PATTERNS.md
@src/hooks/useCameraStream.ts
@src/App.tsx
@src/components/CameraView.tsx
@src/components/OfflineIndicator.tsx
@src/lib/constants.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Camera error inline banner with iOS PWA guidance (CAM-08)</name>
  <files>src/components/CameraView.tsx, src/hooks/useCameraStream.ts, src/App.tsx, src/lib/constants.ts, src/components/CameraView.error.test.tsx</files>
  <read_first>
    src/components/CameraView.tsx
    src/hooks/useCameraStream.ts
    src/App.tsx
    src/lib/constants.ts
    .planning/phases/01-next-js-migration-capture-foundation/01-UI-SPEC.md
  </read_first>
  <behavior>
    - Test: when cameraError prop is non-null, banner renders t.cameraPermissionDenied and t.cameraErrorIosGuide text
    - Test: onDismissCameraError callback fires when dismiss button clicked
    - Test: onRetryCamera calls startCamera when retry clicked
  </behavior>
  <action>
    Add clearCameraError useCallback in useCameraStream setting cameraError null; export startCamera and clearCameraError (or dismissCameraError) in return object. Keep demo default on first launch per D-10 — do not auto-call startCamera on mount.

    Extend I18N cn/en per UI-SPEC Copywriting Contract: revise cameraPermissionDenied headline tone; add cameraErrorIosGuide with Safari Settings and Add to Home Screen reinstall guidance per D-08.

    Add CameraView props cameraError, onRetryCamera, onDismissCameraError. When cameraError non-null, render inline banner above shutter prompt in bottom column per UI-SPEC section 1 — AlertCircle icon, headline, body, Retry text button calling onRetryCamera, dismiss X button calling onDismissCameraError. Styling rounded-2xl border amber bg slate-900/90 backdrop-blur; z-20 inside CameraView not fixed full screen per D-07.

    Wire props in App.tsx from useCameraStream destructuring. Do not log-only errors — banner must render whenever cameraError set after failed startCamera.

    Implement CameraView.error.test.tsx with @testing-library/react.
  </action>
  <acceptance_criteria>
    - cameraError wired App → CameraView
    - Banner visible with I18N strings when error set
    - CameraView.error.test.tsx passes all tests
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/components/CameraView.error.test.tsx</automated>
    <fails_when>non-zero exit or skipped tests</fails_when>
    <automated>grep -c 'cameraErrorIosGuide' src/lib/constants.ts | grep -qv '^0$'</automated>
    <fails_when>iOS guide I18N key missing</fails_when>
  </verify>
  <done>CAM-08 satisfied — camera failures visible in UI with iOS PWA recovery copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Offline indicator i18n and behavior (PWA-03)</name>
  <files>src/components/OfflineIndicator.tsx, src/App.tsx, src/lib/constants.ts, src/components/OfflineIndicator.test.tsx</files>
  <read_first>
    src/components/OfflineIndicator.tsx
    src/App.tsx
    src/lib/constants.ts
    .planning/phases/01-next-js-migration-capture-foundation/01-UI-SPEC.md
  </read_first>
  <behavior>
    - Test: renders null when navigator.onLine true
    - Test: renders offlineMode I18N text when navigator.onLine false
  </behavior>
  <action>
    Add offlineMode key to I18N cn/en per UI-SPEC. Change OfflineIndicator to accept lang Language prop; replace hardcoded bilingual string with t.offlineMode. Pass lang from App.tsx where OfflineIndicator is rendered.

    Keep existing online/offline event listeners and fixed bottom-3 left-4 z-50 styling. Verify z-index stacking: offline pill z-50, camera error banner z-20 inside view — no overlap at default viewport.

    Implement OfflineIndicator.test.tsx mocking navigator.onLine and dispatchEvent offline/online.
  </action>
  <acceptance_criteria>
    - OfflineIndicator uses I18N[lang].offlineMode
    - Component hidden when online, visible when offline
    - OfflineIndicator.test.tsx passes
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/components/OfflineIndicator.test.tsx</automated>
    <fails_when>non-zero exit or skipped tests</fails_when>
  </verify>
  <done>PWA-03 offline indicator uses i18n and responds to connectivity events.</done>
</task>

<task type="auto">
  <name>Phase 1 verification gate — full suite and build smoke</name>
  <files>.planning/phases/01-next-js-migration-capture-foundation/01-VALIDATION.md</files>
  <read_first>
    .planning/phases/01-next-js-migration-capture-foundation/01-VALIDATION.md
    .planning/ROADMAP.md
    package.json
  </read_first>
  <action>
    Run full verification pass: bun run lint, bun run build, bun run test (all test files green, zero skipped). Confirm dist/sw.js precache still shell-only. Update 01-VALIDATION.md frontmatter nyquist_compliant true and wave_0_complete true when all automated commands green.

    Confirm PWA-01 readiness: build artifacts include manifest.json, sw.js, icons; document in SUMMARY that human should verify install + offline shell on Vercel preview per VALIDATION manual table (no checkpoint task — embed human-check in verify).

    Run complete test suite ensuring all seven Phase 1 test files pass without skip.
  </action>
  <acceptance_criteria>
    - bun run lint, build, and test all exit 0
    - All Phase 1 test files have zero it.skip remaining
    - 01-VALIDATION.md nyquist_compliant set true
  </acceptance_criteria>
  <verify>
    <automated>bun run lint && bun run build && bun run test</automated>
    <fails_when>any command exits non-zero</fails_when>
    <automated>rg 'it\.skip' src/App.capture.test.ts src/App.processing.test.ts src/App.baseline.test.ts src/hooks/useCameraStream.capture.test.ts src/hooks/useCameraStream.torch.test.ts src/components/CameraView.error.test.tsx src/components/OfflineIndicator.test.tsx ; test $? -eq 1</automated>
    <fails_when>skipped tests remain in Phase 1 test files</fails_when>
  </verify>
  <done>Phase 1 automated verification green; ready for /gsd-verify-work and Vercel deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser permissions | Camera denial surfaces to user via banner |
| navigator.onLine | Connectivity signal drives offline UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Spoofing | cameraError message display | low | accept | Display browser error message as headline; iOS guide is static I18N copy |
| T-01-01 | Tampering | Offline SW shell | high | mitigate | Verified in 01-02 build smoke; manual PWA install check on Vercel |
</threat_model>

<verification>
Full lint + build + test suite. Manual: Vercel preview PWA install and offline shell (PWA-01).
</verification>

<success_criteria>
CAM-08 and PWA-03 complete with tests; all 14 Phase 1 requirement IDs covered across plans 01-01 through 01-04.
</success_criteria>

## Artifacts this phase produces

- clearCameraError — dismiss handler exported from useCameraStream
- CameraView error banner block — inline CAM-08 UI with Retry and Dismiss
- OfflineIndicator lang prop — i18n connectivity pill
- I18N keys cameraErrorIosGuide, offlineMode — constants.ts bilingual strings

<output>
Create `.planning/phases/01-next-js-migration-capture-foundation/01-04-SUMMARY.md` when done
</output>
