---
phase: 01-next-js-migration-capture-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - vercel.json
  - src/hooks/useCameraStream.ts
  - src/App.tsx
  - package.json
  - README.md
autonomous: true
requirements:
  - TECH-01
  - TECH-02
  - TECH-03
  - TECH-05
  - TECH-07
  - CAM-09
  - PWA-01
  - PWA-02
user_setup:
  - service: vercel
    why: Production and preview deployments (D-03)
    dashboard_config:
      - task: Connect Git repository to Vercel project
        location: "Vercel Dashboard - Add New Project - Import Git Repository"
      - task: Set production branch to main; enable preview deployments on pull requests
        location: "Vercel Dashboard - Project Settings - Git"
estimate:
  tokens: 55000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - bun run build emits dist/sw.js, dist/registerSW.js, dist/manifest.json, and hashed dist/assets/*
    - vercel.json SPA rewrite sends unknown paths to index.html per D-04
    - vercel.json Cache-Control no-store on HTML, sw.js, registerSW.js, manifest.json per D-06
    - vercel.json Cache-Control immutable on /assets/* and /workbox-* per D-06
    - Demo mode captureFrame draws baseline.imageDataUrl displayed in CameraView, not CDN constant per D-11/CAM-09
    - "package.json name is shelfguard; server AI deps removed per D-19/TECH-03"
    - lucide-react remains the icon library across components per TECH-05
    - Service Worker precache list contains app shell assets only — no user photo blob URLs per PWA-02
    - vite-plugin-pwa registerType remains autoUpdate per D-05
  artifacts:
    - vercel.json
  key_links:
    - App.tsx handleShutterClick passes baseline.imageDataUrl into captureFrame
    - useCameraStream captureFrame demo branch draws passed baselineImageUrl
    - vercel.json header paths match actual dist/ output (manifest.json not manifest.webmanifest)
  prohibitions:
    - "Must not migrate to Next.js or add serwist-next in this plan per D-01"
    - "Must not add runtime SW caching routes for user blob or data URLs per PWA-02"
    - "Must not install opencv-js WASM bundle per D-12"
---

# Plan 01-02: Vercel Deploy Tracer + Demo Capture Fix

**As a** retail field rep,
**I want** the app deployable to Vercel with reliable demo-mode capture,
**so that** I can complete one inspection cycle on a production-like URL with correct frame snapshots.

<objective>
Wire the thinnest end-to-end deploy path: vercel.json + production build + demo capture fix proving CAM-09, plus dependency hygiene for pure-client constraint.

Purpose: Walking Skeleton tracer — proves Vite PWA deploy stack before FSM/camera UX expansion.
Output: vercel.json, working build artifacts, fixed captureFrame, cleaned package.json, updated README.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-next-js-migration-capture-foundation/SKELETON.md
@.planning/phases/01-next-js-migration-capture-foundation/01-CONTEXT.md
@.planning/phases/01-next-js-migration-capture-foundation/01-RESEARCH.md
@.planning/phases/01-next-js-migration-capture-foundation/01-PATTERNS.md
@src/hooks/useCameraStream.ts
@src/App.tsx
@vite.config.ts
@src/components/CameraView.tsx
</context>

<tasks>

<task type="tracer">
  <name>End-to-end Vercel build path + demo capture fix (CAM-09)</name>
  <files>vercel.json, src/hooks/useCameraStream.ts, src/App.tsx</files>
  <read_first>
    .planning/phases/01-next-js-migration-capture-foundation/01-RESEARCH.md
    .planning/phases/01-next-js-migration-capture-foundation/01-PATTERNS.md
    src/hooks/useCameraStream.ts
    src/App.tsx
    src/components/CameraView.tsx
    vite.config.ts
  </read_first>
  <reversibility rating="costly">vercel.json cache header patterns affect every deploy; wrong paths cause stale SW or MIME errors until redeployed.</reversibility>
  <action>
    Create root vercel.json per D-04/D-06 and RESEARCH Pattern 1: SPA rewrite source /(.*) to /index.html; headers with Cache-Control no-store for paths matching HTML files, /sw.js, /registerSW.js, /manifest.json (include Content-Type application/manifest+json on manifest); immutable max-age on /assets/(.*) and /workbox-(.*). Use build-verified paths from dist/ — manifest is manifest.json not manifest.webmanifest.

    Fix demo capture per D-11: change captureFrame signature to accept baselineImageUrl string parameter. In demo branch (isUsingDemoFeed true), create Image, set src to baselineImageUrl, draw to 1080x1920 canvas when loaded; return canvas JPEG data URL. For live video branch keep existing videoRef draw. Update App.tsx handleShutterClick to call captureFrame(baseline.imageDataUrl) instead of captureFrame() with no args.

    Run bun run build. Confirm dist contains sw.js, registerSW.js, manifest.json, index.html, assets/. Grep dist/sw.js precache entries — confirm only shell assets (index.html, hashed JS/CSS, registerSW.js, workbox) and no blob or user image patterns.

    Keep vite.config.ts VitePWA registerType autoUpdate and manifest false unchanged per D-05.
  </action>
  <acceptance_criteria>
    - vercel.json exists with rewrite and PWA-safe headers on manifest.json and sw.js
    - captureFrame accepts baselineImageUrl and demo branch returns canvas data URL derived from that URL
    - App.tsx passes baseline.imageDataUrl to captureFrame on shutter
    - dist/sw.js exists after build; precache list has no blob: or data: URL patterns
  </acceptance_criteria>
  <verify>
    <automated>bun run build && test -f dist/sw.js && test -f dist/manifest.json</automated>
    <fails_when>build fails or SW/manifest artifacts missing</fails_when>
    <automated>grep -v '^#' dist/sw.js | grep -cE 'blob:|data:image' | grep -qx '0'</automated>
    <fails_when>precache references user image blob or data URLs</fails_when>
    <automated>grep -c 'captureFrame(baseline.imageDataUrl)' src/App.tsx | grep -qv '^0$'</automated>
    <fails_when>App.tsx does not pass baseline URL to captureFrame</fails_when>
  </verify>
  <done>Production build succeeds; demo capture snapshots displayed baseline frame; vercel.json ready for Git deploy.</done>
</task>

<task type="auto" tdd="true">
  <name>Implement CAM-09 capture unit test</name>
  <files>src/hooks/useCameraStream.capture.test.ts, src/hooks/useCameraStream.ts</files>
  <read_first>
    src/hooks/useCameraStream.capture.test.ts
    src/hooks/useCameraStream.ts
    .planning/phases/01-next-js-migration-capture-foundation/01-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: When isUsingDemoFeed is true, captureFrame(customDataUrl) return value is a data:image/jpeg URL, not the DEFAULT_SHELF_IMAGE_URL constant string
    - Test 2: captureFrame uses the passed baselineImageUrl argument in demo mode (mock Image draw or spy on canvas drawImage source)
  </behavior>
  <action>
    Replace it.skip in src/hooks/useCameraStream.capture.test.ts with real tests using vitest + jsdom. Export captureFrame logic testably — either test via renderHook from @testing-library/react wrapping useCameraStream, or extract pure canvas helper if hook testing is impractical. Assert demo capture output differs from hardcoded CDN constant when a distinct test data URL is passed.
  </action>
  <acceptance_criteria>
    - npx vitest run src/hooks/useCameraStream.capture.test.ts passes with zero skipped tests
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/hooks/useCameraStream.capture.test.ts</automated>
    <fails_when>non-zero exit or skipped tests remain</fails_when>
  </verify>
  <done>CAM-09 demo capture behavior has automated regression test.</done>
</task>

<task type="auto">
  <name>Remove server deps and update README for Vercel (TECH-03, D-19, D-20)</name>
  <files>package.json, README.md</files>
  <read_first>
    package.json
    README.md
    .planning/phases/01-next-js-migration-capture-foundation/01-CONTEXT.md
  </read_first>
  <action>
    Remove dependencies @google/genai, express, dotenv, motion and devDependency @types/express from package.json; run bun remove or npm uninstall to update lockfile. Rename package name from react-example to shelfguard per D-20.

    Rewrite README.md: ShelfGuard title and description; local dev with bun install (or npm install) and bun run dev; build with bun run build; Vercel deploy steps — connect Git repo, framework preset Vite, output dist, production branch main, preview on PRs per D-02/D-03. Remove Gemini API key and AI Studio as primary setup path. Document that camera requires HTTPS (Vercel prod or localhost).

    Verify zero imports of removed packages under src/ — TECH-02 satisfied by existing SPA with no server code.
  </action>
  <acceptance_criteria>
    - package.json name is shelfguard
    - package.json dependencies do not list genai, express, dotenv, or motion
    - package.json dependencies include lucide-react per TECH-05
    - README mentions Vercel deploy and does not require GEMINI_API_KEY for core app
    - rg finds no server package imports under src/
    - CameraView.tsx imports from lucide-react (icon library unchanged)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.name!=='shelfguard') process.exit(1); const bad=['@google/genai','express','dotenv','motion']; if(bad.some(x=>(p.dependencies||{})[x])) process.exit(1)"</automated>
    <fails_when>package name wrong or forbidden deps still listed</fails_when>
    <automated>rg '@google/genai|from \"express\"|from \"dotenv\"|from \"motion\"' src/ ; test $? -eq 1</automated>
    <fails_when>server or AI imports found under src/</fails_when>
    <automated>grep -c 'lucide-react' package.json | grep -qv '^0$'</automated>
    <fails_when>lucide-react missing from package.json dependencies (TECH-05)</fails_when>
    <automated>grep -c 'from \"lucide-react\"' src/components/CameraView.tsx | grep -qv '^0$'</automated>
    <fails_when>CameraView no longer imports lucide-react icons</fails_when>
  </verify>
  <done>TECH-03 pure-client deps enforced; TECH-05 lucide-react retained; README documents ShelfGuard + Vercel path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN (Vercel) → browser | Static assets and cache headers cross trust boundary |
| npm registry → package.json | Dependency removal/install |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Stale index.html / sw.js | high | mitigate | D-06 no-store headers in vercel.json task |
| T-01-05 | Information disclosure | Removed AI keys in bundle | medium | mitigate | D-19 removes @google/genai and dotenv from dependencies |
| T-01-SC | Tampering | npm/bun install | medium | accept | Only removals + devDeps from prior plan; no new runtime packages |
</threat_model>

<verification>
bun run lint && bun run build && bun run test. Inspect dist/sw.js precache manually for PWA-01 shell-only caching story.
</verification>

<success_criteria>
Deployable Vite PWA build with vercel.json; demo capture uses displayed frame; server deps removed; TECH-05 lucide-react unchanged in use.
</success_criteria>

## Artifacts this phase produces

- vercel.json — Vercel SPA rewrite and PWA cache-control headers
- captureFrame(baselineImageUrl) — updated hook API for demo frame snapshot
- package.json name shelfguard — renamed from react-example

<output>
Create `.planning/phases/01-next-js-migration-capture-foundation/01-02-SUMMARY.md` when done
</output>
