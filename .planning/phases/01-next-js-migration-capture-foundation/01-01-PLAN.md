---
phase: 01-next-js-migration-capture-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - package.json
  - vitest.config.ts
  - vitest.setup.ts
  - src/test/setup.ts
autonomous: true
requirements:
  - TECH-01
  - TECH-07
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - REQUIREMENTS.md TECH-01 describes Vite 8 + React 19 + TypeScript + Tailwind 4 (not Next.js App Router) per D-01
    - REQUIREMENTS.md TECH-07 describes vite-plugin-pwa Service Worker app-shell caching (not @serwist/next) per D-01
    - ROADMAP Phase 1 title and success criteria reference Vite PWA on Vercel, not Next.js migration
    - Vitest runs with jsdom and @/ path alias matching vite.config.ts
    - Out of Scope in REQUIREMENTS.md no longer lists Vite production build as migrated-away
    - D-13: v1 vision path locked to @techstark/opencv-js only; pixelmatch excluded from v1 — Phase 4 planner must not offer pixelmatch (cross-phase note in REQUIREMENTS Out of Scope)
    - D-21: metadata.json AI Studio boilerplate left unchanged in Phase 1 unless zero-cost edit during README pass
  artifacts:
    - vitest.config.ts
    - vitest.setup.ts
  key_links:
    - vitest.config.ts resolve.alias @ matches vite.config.ts alias
  prohibitions:
    - Must not add Next.js, @serwist/next, or App Router dependencies in Phase 1 planning artifacts per D-01
    - Must not install @techstark/opencv-js or Web Worker vision scaffold in Wave 0 per D-12
---

# Plan 01-01: Docs Reconciliation & Test Scaffold

**As a** retail field rep building on ShelfGuard,
**I want** planning docs and test infrastructure aligned to the locked Vite + Vercel stack,
**so that** downstream implementation tasks do not chase stale Next.js requirements.

<objective>
Reconcile REQUIREMENTS.md and ROADMAP.md with CONTEXT.md D-01 stack pivot, then install Vitest + jsdom so every subsequent plan has automated verify targets.

Purpose: Eliminate Next.js drift before code changes land; satisfy VALIDATION.md Wave 0 gaps.
Output: Updated planning docs, vitest.config.ts, empty/passing test scaffold.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-next-js-migration-capture-foundation/01-CONTEXT.md
@.planning/phases/01-next-js-migration-capture-foundation/01-RESEARCH.md
@.planning/phases/01-next-js-migration-capture-foundation/01-VALIDATION.md
@vite.config.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Sync REQUIREMENTS.md and ROADMAP.md to Vite stack (D-01)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/phases/01-next-js-migration-capture-foundation/01-CONTEXT.md
  </read_first>
  <reversibility rating="costly">Rewriting TECH-01/TECH-07 and Phase 1 goal touches traceability for all 54 requirements; reverting would re-confuse downstream phases.</reversibility>
  <action>
    Update REQUIREMENTS.md per D-01: rewrite TECH-01 to state the app is built with Vite 8 + React 19 + TypeScript + Tailwind CSS 4 (remove Next.js App Router wording). Rewrite TECH-02 to state all camera/Canvas/IndexedDB/device API code runs client-side in the Vite SPA mount tree (remove "use client" / Server Component wording — note TECH-02 is satisfied implicitly by SPA architecture per RESEARCH A5). Rewrite TECH-07 to state PWA offline capability via vite-plugin-pwa with registerType autoUpdate; Service Worker caches app shell only. Remove the Out of Scope row that says Vite production build migrates to Next.js — replace with note that Next.js migration is deferred per CONTEXT deferred ideas. Update traceability table descriptions if they mention Next.js.

    Update ROADMAP.md Phase 1: rename to Capture Foundation & Vercel Deploy (keep directory slug unchanged). Rewrite Goal to match CONTEXT domain boundary — Vite pure-client PWA on Vercel, FSM/camera bugs fixed, no Next.js migration. Rewrite all seven Success Criteria bullets to remove Next.js / @serwist/next references; cite Vite SPA client-only, vite-plugin-pwa SW, vercel.json deploy. Update Overview phase bullet and Progress table row label similarly. Do not change phase requirement ID list or phase numbering.

    Do not add Next.js tasks, dependencies, or success criteria anywhere.
  </action>
  <acceptance_criteria>
    - grep for Next.js in TECH-01 and TECH-07 requirement lines returns zero matches
    - ROADMAP Phase 1 Goal contains Vite and Vercel and does not contain Next.js
    - Out of Scope no longer forbids Vite production build in favor of Next.js migration
  </acceptance_criteria>
  <verify>
    <automated>grep -E 'TECH-01|TECH-07' .planning/REQUIREMENTS.md | grep -c 'Next.js' | grep -qx '0'</automated>
    <fails_when>TECH-01 or TECH-07 lines still contain Next.js wording</fails_when>
    <automated>grep -A5 'Phase 1:' .planning/ROADMAP.md | grep -c 'Next.js' | grep -qx '0'</automated>
    <fails_when>Phase 1 ROADMAP section still references Next.js migration</fails_when>
    <automated>grep 'Phase 1:' .planning/ROADMAP.md | grep -c 'Vite\|Vercel' | grep -qv '^0$'</automated>
    <fails_when>Phase 1 section lacks Vite or Vercel reference</fails_when>
  </verify>
  <done>TECH-01, TECH-07, and ROADMAP Phase 1 goal/success criteria describe Vite + vite-plugin-pwa + Vercel per D-01.</done>
</task>

<task type="auto">
  <name>Install Vitest + jsdom test scaffold</name>
  <files>package.json, vitest.config.ts, vitest.setup.ts</files>
  <read_first>
    package.json
    vite.config.ts
    .planning/phases/01-next-js-migration-capture-foundation/01-VALIDATION.md
    .planning/phases/01-next-js-migration-capture-foundation/01-RESEARCH.md
  </read_first>
  <action>
    Add devDependencies: vitest, jsdom, @testing-library/react, @testing-library/jest-dom (use bun add -d if bun.lock present, else npm install -D). Create vitest.config.ts exporting defineConfig with environment jsdom, globals true, setupFiles pointing to vitest.setup.ts, and resolve.alias @ mapped to project root matching vite.config.ts path.resolve(__dirname, '.'). Create vitest.setup.ts importing @testing-library/jest-dom/vitest. Add npm script "test": "vitest run" to package.json scripts alongside existing lint/build. Do not add production dependencies.
  </action>
  <acceptance_criteria>
    - vitest.config.ts exists with jsdom environment and @ alias
    - package.json has test script invoking vitest run
    - npx vitest run --passWithNoTests exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f vitest.config.ts && bun run test -- --passWithNoTests</automated>
    <fails_when>config missing or vitest exits non-zero</fails_when>
  </verify>
  <done>Vitest 3.x scaffold runs with jsdom; Wave 0 MISSING test file gates can resolve in later plans.</done>
</task>

<task type="auto">
  <name>Seed placeholder test files for Phase 1 verify map</name>
  <files>src/hooks/useCameraStream.capture.test.ts, src/hooks/useCameraStream.torch.test.ts, src/App.capture.test.ts, src/App.processing.test.ts, src/App.baseline.test.ts, src/components/CameraView.error.test.tsx, src/components/OfflineIndicator.test.tsx</files>
  <read_first>
    .planning/phases/01-next-js-migration-capture-foundation/01-VALIDATION.md
    vitest.config.ts
  </read_first>
  <action>
    Create minimal test file stubs that import vitest describe/it/expect and export empty or skipped suites so file paths from VALIDATION.md exist: src/hooks/useCameraStream.capture.test.ts, src/hooks/useCameraStream.torch.test.ts, src/App.capture.test.ts, src/App.processing.test.ts, src/App.baseline.test.ts, src/components/CameraView.error.test.tsx, src/components/OfflineIndicator.test.tsx. Each file contains at least one it.skip placeholder describing the requirement it will cover (STAB-01, STAB-02, STAB-03, STAB-04, CAM-08, CAM-09, PWA-03). Do not implement assertions yet — later plans replace skips with real tests.
  </action>
  <acceptance_criteria>
    - All seven test file paths from 01-VALIDATION.md Per-Task Verification Map exist
    - bun run test completes without import errors
  </acceptance_criteria>
  <verify>
    <automated>test -f src/App.capture.test.ts && test -f src/components/OfflineIndicator.test.tsx && bun run test</automated>
    <fails_when>any listed test file missing or vitest fails on import</fails_when>
  </verify>
  <done>Test file paths exist; full suite runs green with skipped placeholders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs → executor | REQUIREMENTS/ROADMAP edits constrain all downstream tasks |
| npm registry → devDependencies | Vitest install pulls packages from npm |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | dist/sw.js stale cache | high | mitigate | D-06 no-store headers planned in 01-02 vercel.json |
| T-01-SC | Tampering | npm/bun devDependency install | medium | mitigate | Package Legitimacy Audit in RESEARCH.md — vitest/jsdom are OK verdict |
</threat_model>

<verification>
Run bun run lint && bun run test after all tasks. Confirm REQUIREMENTS TECH-01/07 and ROADMAP Phase 1 no longer mention Next.js.
</verification>

<success_criteria>
Planning artifacts and test scaffold align to Vite stack; Vitest runs green with skipped placeholders.
</success_criteria>

## Artifacts this phase produces

- vitest.config.ts — Vitest configuration with jsdom and @ alias
- vitest.setup.ts — jest-dom setup for component tests
- src/hooks/useCameraStream.capture.test.ts — CAM-09 test scaffold
- src/hooks/useCameraStream.torch.test.ts — STAB-02 test scaffold
- src/App.capture.test.ts — STAB-01 test scaffold
- src/App.processing.test.ts — STAB-03 test scaffold
- src/App.baseline.test.ts — STAB-04 test scaffold
- src/components/CameraView.error.test.tsx — CAM-08 test scaffold
- src/components/OfflineIndicator.test.tsx — PWA-03 test scaffold
- package.json test script — npm run test / bun run test entry point

<output>
Create `.planning/phases/01-next-js-migration-capture-foundation/01-01-SUMMARY.md` when done
</output>
