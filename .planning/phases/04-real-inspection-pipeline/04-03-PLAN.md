---
phase: 04-real-inspection-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/App.tsx
  - src/App.firstBaseline.integration.test.tsx
  - src/App.completeAudit.integration.test.tsx
  - src/App.processing.integration.test.tsx
  - src/lib/vision.ts
autonomous: true
requirements:
  - VIS-04
  - RSLT-06
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - First-baseline integration test proves shutter on empty shelf routes ROI_CONFIG without analyzeShelfCapture call (D-01, ROI-01)
    - PROCESSING overlay appears when worker exceeds 800ms during scan path (VIS-04, STAB-03)
    - Complete inspection saves audit with dismissed anomaly flags and returns to current shelf CAMERA_IDLE (D-26, RSLT-06)
    - Worker error shows analysis-failed message and returns to CAMERA_IDLE — not infinite PROCESSING (04-UI-SPEC error vision-worker)
    - Full phase gate lint + build + test suite green
    - opencv-worker-init cold start covered by PROCESSING overlay — no separate init UI (04-UI-SPEC unresolved backstop)
  artifacts:
    - src/App.firstBaseline.integration.test.tsx
    - src/App.completeAudit.integration.test.tsx
    - src/App.processing.integration.test.tsx
  key_links:
    - handleCompleteAudit appendAuditRecord with anomalies including dismissed true
    - handleShutterClick PROCESSING timer at 800ms when analysisPromise pending
    - analyzeShelfCapture error rejection triggers App error toast and CAMERA_IDLE
---

# Plan 04-03: Integration Tests & Phase Verification Gate

**As a** retail field rep finishing an inspection,
**I want to** complete audit and return to my shelf camera with confidence the full pipeline is tested,
**so that** first-baseline routing, worker timing, and audit persistence work reliably in CI.

<objective>
Lock first-baseline FSM and PROCESSING timing with integration tests; harden worker error path; run full phase verification gate covering RSLT-06 and remaining VIS-04 assertions.

Purpose: Phase 4 sign-off — all 16 requirement IDs verified automated where possible.
Output: App.firstBaseline.integration.test.tsx green, processing test updated for real worker path, full suite green.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-real-inspection-pipeline/04-CONTEXT.md
@.planning/phases/04-real-inspection-pipeline/04-VALIDATION.md
@.planning/phases/04-real-inspection-pipeline/04-02-SUMMARY.md
@src/App.processing.integration.test.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>First-baseline FSM integration test — no scan, no diff (D-01, ROI-01)</name>
  <files>src/App.firstBaseline.integration.test.tsx</files>
  <read_first>
    src/App.processing.integration.test.tsx
    src/App.shelfIsolation.integration.test.tsx
    .planning/phases/04-real-inspection-pipeline/04-PATTERNS.md
  </read_first>
  <behavior>
    - loadBaselineRaw returns null → shutter click sets app to ROI setup view
    - analyzeShelfCapture mock not called on first-baseline shutter path
    - Scanning animation overlay text not present after first-baseline shutter
    - RoiSetupView calibration background uses captured frame URL
  </behavior>
  <action>
    Create src/App.firstBaseline.integration.test.tsx following App.processing.integration.test.tsx mock stack: vi.mock vision, useCameraStream, useDeviceOrientation, usePWAInstall, storage, shelfStorage. Set loadBaselineRaw to vi.fn(async () => null) and hasPersistedBaseline false path.

    Render App, trigger shutter via existing test helper or data-testid shutter button. Assert ROI setup visible (calibration header or tier dividers). Assert analyzeShelfCapture not called. Assert no scan overlay processing pill text 正在进行透视配准.

    Use vi.useFakeTimers where needed; do not mock first-baseline away — test the real FSM branch from 04-02.
  </action>
  <verify>
    <automated>bun run test -- src/App.firstBaseline.integration.test.tsx -x</automated>
    <fails_when>first-baseline integration test fails or file missing</fails_when>
  </verify>
  <acceptance_criteria>
    - Empty shelf shutter opens ROI calibration without calling vision
    - No SCANNING_ANIM state on first-baseline path
  </acceptance_criteria>
  <done>First-baseline routing locked by integration test.</done>
</task>

<task type="auto" tdd="true">
  <name>Worker error path and complete-inspection shelf return (D-26, RSLT-06, 04-UI-SPEC error)</name>
  <files>src/App.tsx, src/lib/vision.ts, src/App.completeAudit.integration.test.tsx</files>
  <read_first>
    src/App.tsx
    src/App.shelfIsolation.integration.test.tsx
    src/components/ResultInspectView.tsx
    .planning/phases/04-real-inspection-pipeline/04-UI-SPEC.md
  </read_first>
  <behavior>
    - Complete inspection button calls appendAuditRecord with anomalies including dismissed true flags
    - After audit save, app returns to CAMERA_IDLE on current activeShelfId — shelf context unchanged
    - Worker error rejection shows analysis-failed copy and returns to CAMERA_IDLE
  </behavior>
  <action>
    Harden handleShutterClick catch path: when analyzeShelfCapture rejects (worker error postMessage), clear scan timer, show toast or inline banner with I18N analysis-failed copy CN 分析失败，请重试拍摄 / EN Analysis failed — tap shutter to retry, setAppMode CAMERA_IDLE, release capture lock. Do not leave user stuck in PROCESSING.

    Verify handleCompleteAudit: saves AuditRecord with current anomalies array including dismissed true flags per D-23, tolerance as numeric value, returns to active shelf CAMERA_IDLE after confetti delay — not global shelf reset. Confirm setAppMode CAMERA_IDLE uses current activeShelfId context.

    Pre-warm worker per RESEARCH Q1 (RESOLVED): on first CAMERA_IDLE entry when hasPersistedBaseline true, spawn worker and await cvReadyPromise init — no separate init UI; PROCESSING overlay covers any remaining cold-start delay beyond 800ms.

    Create src/App.completeAudit.integration.test.tsx following App.shelfIsolation.integration.test.tsx mock stack with persisted baseline and mocked analyzeShelfCapture returning anomalies with one dismissed. Render App, shutter through to RESULT_INSPECT, dismiss an anomaly, click complete-inspection button, advance confetti timer if needed. Assert appendAuditRecord called with anomalies array containing dismissed true on dismissed id, tolerance as number, and app returns to camera view on same activeShelfId (RSLT-06, D-26).
  </action>
  <verify>
    <automated>bun run test -- src/App.completeAudit.integration.test.tsx -x</automated>
    <fails_when>complete-inspection integration test fails or file missing</fails_when>
    <automated>grep -E '分析失败|Analysis failed' src/App.tsx src/lib/i18n.ts 2>/dev/null | grep -c . | grep -qv '^0$' || grep -c 'analysisFailed\|analysis-failed' src/App.tsx src/lib/i18n.ts 2>/dev/null | grep -qv '^0$'</automated>
    <fails_when>worker error user-facing copy not added</fails_when>
  </verify>
  <acceptance_criteria>
    - Worker failure returns user to camera with documented error copy
    - Complete inspection persists audit and returns to current shelf camera
    - Dismissed anomalies saved with dismissed true in audit record
  </acceptance_criteria>
  <done>Audit completion and worker error UX hardened for production path.</done>
</task>

<task type="auto">
  <name>Phase 4 verification gate — PROCESSING timing + full suite (VIS-04, all reqs)</name>
  <files>src/App.processing.integration.test.tsx</files>
  <read_first>
    src/App.processing.integration.test.tsx
    .planning/phases/04-real-inspection-pipeline/04-VALIDATION.md
    .planning/phases/03-guided-capture-quality/03-03-SUMMARY.md
  </read_first>
  <action>
    Update src/App.processing.integration.test.tsx if needed: mock analyzeShelfCapture with 1200ms delay, assert PROCESSING overlay appears after 800ms fake timer advance while hasPersistedBaseline true. Retain existing STAB-03 behavior with real worker available in non-mocked unit tests.

    Run full phase gate matching Phase 3 proven commands: lint, build, full test suite. Ensure new Phase 4 test files included: visionWorker.integration.test.ts, toleranceParams.test.ts, bboxUtils.test.ts, classifyContour.test.ts, ResultInspectView.tolerance.test.tsx, App.toleranceReDiff.integration.test.tsx, App.firstBaseline.integration.test.tsx, App.completeAudit.integration.test.tsx, App.processing.integration.test.tsx.

    Document manual-only checkpoints in SUMMARY: aligned device capture (VIS-01 drift), blink compare mobile gesture (RSLT-02) per 04-VALIDATION.md — not blocking automated gate.
  </action>
  <verify>
    <automated>bun run test -- src/App.processing.integration.test.tsx -x</automated>
    <fails_when>PROCESSING integration test fails</fails_when>
    <automated>bun run lint && bun run build && bun run test</automated>
    <fails_when>phase gate lint build or full test suite fails</fails_when>
  </verify>
  <acceptance_criteria>
    - PROCESSING appears when mocked analysis exceeds 800ms
    - bun run lint && bun run build && bun run test all pass
    - All 16 Phase 4 requirement IDs have automated or documented manual coverage
  </acceptance_criteria>
  <done>Phase 4 automated verification gate green; manual device checkpoints documented in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Complete audit → IndexedDB | Anomaly array with dismissed flags persisted locally |
| Worker error → UI | Error message must not leak WASM stack traces to user |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-08 | Denial of Service | Stuck PROCESSING state | medium | mitigate | catch analyzeShelfCapture reject; clear timer; force CAMERA_IDLE |
| T-4-09 | Tampering | Audit record anomalies | low | accept | Local-only PWA; no remote trust boundary |

</threat_model>

<verification>
bun run test -- src/App.firstBaseline.integration.test.tsx src/App.completeAudit.integration.test.tsx src/App.processing.integration.test.tsx -x
bun run lint && bun run build && bun run test
</verification>

<success_criteria>
First-baseline and PROCESSING integration tests pass; complete inspection returns to shelf camera; full lint/build/test gate green.
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| src/App.firstBaseline.integration.test.tsx | 04-03 | ROI-01 / D-01 FSM regression lock |
| src/App.completeAudit.integration.test.tsx | 04-03 | RSLT-06 / D-26 complete-inspection flow |
| src/App.processing.integration.test.tsx | 04-03 | VIS-04 / STAB-03 timing verification |
| 04-03-SUMMARY.md | 04-03 | Phase gate results + manual checkpoint list |

<output>
Create `.planning/phases/04-real-inspection-pipeline/04-03-SUMMARY.md` when done
</output>

<!-- Multi-Source Coverage Audit (04-03 slice + phase complete) -->
<!-- VIS-04, RSLT-06 — COVERED in 04-03 -->
<!-- GOAL: real inspection pipeline — COVERED across 04-01–04-03 -->
<!-- All 16 REQ IDs distributed; all D-01–D-28 locked decisions covered except Claude's Discretion items -->
<!-- Deferred: homography VIS-07, SHLF-05 INITIAL_GUIDE, pixelmatch — excluded per CONTEXT deferred -->
