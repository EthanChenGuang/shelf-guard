---
phase: 04-real-inspection-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - public/test-fixtures/baseline-aligned.jpg
  - public/test-fixtures/capture-missing.jpg
  - public/test-fixtures/capture-displaced.jpg
  - src/workers/visionWorker.ts
  - src/lib/vision.ts
  - src/lib/vision/toleranceParams.ts
  - src/lib/vision/tierGeometry.ts
  - src/lib/vision/bboxUtils.ts
  - src/lib/vision/complianceStats.ts
  - src/lib/vision/toleranceParams.test.ts
  - src/lib/vision/bboxUtils.test.ts
  - src/lib/vision/classifyContour.test.ts
  - src/workers/visionWorker.integration.test.ts
autonomous: false
requirements:
  - TECH-06
  - VIS-01
  - VIS-02
  - VIS-04
  - RSLT-01
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - analyzeShelfCapture returns real OpenCV per-tier diff results — not INITIAL_MOCK_ANOMALIES (D-10, VIS-01)
    - Worker loads @techstark/opencv-js WASM inside visionWorker.ts only — never on main thread (D-19, TECH-06)
    - Golden fixture pair produces at least one MISSING on capture-missing.jpg and one MOVED on capture-displaced.jpg (D-27, D-28, VIS-02)
    - Anomalies use normalized boundingBox 0.0–1.0 in full-frame coordinates (D-10, VIS-02)
    - Diff runs off main thread via postMessage with transferable ImageBitmaps (D-20, D-21, VIS-04)
    - Scan animation path calls real worker concurrently — existing PROCESSING fallback preserved for >800ms (D-22, STAB-03)
    - Worker failure surfaces analysis-failed copy and returns user to camera — not silent hang (04-UI-SPEC error state)
  artifacts:
    - src/workers/visionWorker.ts
    - src/lib/vision/toleranceParams.ts
    - src/lib/vision/tierGeometry.ts
    - src/lib/vision/bboxUtils.ts
    - src/lib/vision/complianceStats.ts
    - public/test-fixtures/baseline-aligned.jpg
    - public/test-fixtures/capture-missing.jpg
    - public/test-fixtures/capture-displaced.jpg
    - src/workers/visionWorker.integration.test.ts
  key_links:
    - vision.ts decode → postMessage(analyze) → visionWorker per-tier pipeline → InspectionAnalysisResult
    - toleranceToDiffParams feeds worker threshold/minArea/displacementPx (D-12)
    - App handleShutterClick analysisPromise calls analyzeShelfCapture without mock filter path
---

# Plan 04-01: Tracer — Real OpenCV Diff on Golden Fixtures

**As a** retail field rep inspecting a calibrated shelf,
**I want to** see red and yellow boxes from real client-side pixel diff,
**so that** missing and displaced products are detected from my capture — not hardcoded mock data.

<objective>
Replace mock analyzeShelfCapture with @techstark/opencv-js Web Worker per-tier diff (D-06–D-10, D-19–D-22). Prove end-to-end on golden JPEG fixtures before ROI/tolerance UI work in 04-02.

Purpose: Deliver core product value — real vision — and de-risk OpenCV+Vite worker bundling early.
Output: visionWorker.ts, pure vision helpers, golden fixtures, integration test green, mock path removed from production.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-inspection-pipeline/04-CONTEXT.md
@.planning/phases/04-real-inspection-pipeline/04-RESEARCH.md
@.planning/phases/04-real-inspection-pipeline/04-PATTERNS.md
@.planning/phases/04-real-inspection-pipeline/04-UI-SPEC.md
@src/lib/vision.ts
@src/types.ts
@src/lib/constants.ts
@src/App.tsx
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Confirm OpenCV Web Worker protocol lock-in (D-19)</name>
  <read_first>
    .planning/phases/04-real-inspection-pipeline/04-CONTEXT.md
    .planning/phases/04-real-inspection-pipeline/04-RESEARCH.md
  </read_first>
  <decision>
    Lock vision architecture to @techstark/opencv-js v5 inside a dedicated Vite ?worker module with postMessage protocol { type: 'analyze', captureBitmap, baselineBitmap, splitYPercentages, imageDimensions, toleranceValue } → InspectionAnalysisResult. No main-thread OpenCV, no pixelmatch, no @opencvjs/worker alternative.
  </decision>
  <context>
    D-19 marks this one-way: worker message schema becomes the vision API contract for Phase 5 and v2. Reversal requires rewriting vision.ts, worker, and all tests. Package legitimacy audit in RESEARCH.md verdict OK for @techstark/opencv-js@5.0.0-release.1.
  </context>
  <options>
    <option id="A">Proceed with locked stack per D-19/D-13 — @techstark/opencv-js in visionWorker.ts via Vite ?worker import</option>
    <option id="B">Defer OpenCV — blocked; violates TECH-06 and locked Phase 1 D-13</option>
  </options>
  <resume-signal>Type "A" or "approved" to proceed with worker install and tracer implementation</resume-signal>
  <acceptance_criteria>
    - Human confirms option A before bun add @techstark/opencv-js executes
    - Decision recorded in commit message referencing D-19
  </acceptance_criteria>
</task>

<task type="tracer">
  <name>End-to-end real diff on golden fixture pair — one aligned inspect path (D-06–D-10, D-19–D-22, TECH-06, VIS-01, VIS-02)</name>
  <reversibility rating="one-way">Worker message protocol and OpenCV WASM dependency become the vision API contract for all later phases (D-19).</reversibility>
  <files>package.json, bun.lock, public/test-fixtures/baseline-aligned.jpg, public/test-fixtures/capture-missing.jpg, public/test-fixtures/capture-displaced.jpg, src/workers/visionWorker.ts, src/lib/vision.ts, src/lib/vision/toleranceParams.ts, src/lib/vision/tierGeometry.ts, src/lib/vision/bboxUtils.ts, src/lib/vision/complianceStats.ts, src/workers/visionWorker.integration.test.ts</files>
  <read_first>
    src/lib/vision.ts
    src/types.ts
    .planning/phases/04-real-inspection-pipeline/04-RESEARCH.md
    .planning/phases/04-real-inspection-pipeline/04-PATTERNS.md
  </read_first>
  <action>
    Install @techstark/opencv-js@5.0.0-release.1 via bun add. Create src/workers/visionWorker.ts importing cvReadyPromise from @techstark/opencv-js inside worker only (D-19). Use Vite ?worker import from src/lib/vision.ts per RESEARCH Pattern 1.

    Implement pure helpers in src/lib/vision/: toleranceToDiffParams per D-12 (accept numeric 0–100), tierBoundsFromSplits with 2% horizontal edge inset per D-16, pixelRectToNormalized + stableAnomalyId per D-10, computeComplianceStats extracting formula from current vision.ts lines 58–65.

    Worker pipeline per D-06: for each of 4 tiers crop baseline and capture to identical pixel dimensions at baseline resolution, grayscale, GaussianBlur 5×5, absdiff, threshold using diffThreshold from toleranceToDiffParams, morph close 3×3, findContours. Classify MISSING per D-07 (capture void heuristic), MOVED per D-08 (centroid displacement), dedupe prefer MISSING per D-09, cap 8 anomalies per tier by contour area descending. Output normalized boundingBox and rowIndex. Compute standardCount per RESEARCH Q2 (RESOLVED): sum baseline foreground blob counts (contours above minContourArea) across tiers; fallback 24 if sum is zero. Delete all cv.Mat in try/finally.

    Replace src/lib/vision.ts mock body: remove INITIAL_MOCK_ANOMALIES import and enum filter branches. Keep InspectionAnalysisResult interface stable. Change analyzeShelfCapture third param to toleranceValue number (default map legacy callers via toleranceToDiffParams). Main thread: dataUrlToImageBitmap helper, getWorker singleton, postMessage with transferable ImageBitmaps, close bitmaps after resolve/reject, validate toleranceValue 0–100 and splitYPercentages length 4 before post (T-4-01). On worker error postMessage type error, reject with Error for App to handle.

    Create public/test-fixtures/ golden JPEG trio per D-28: baseline-aligned.jpg (1080×1920 shelf grid), capture-missing.jpg (same framing with one tier void/product removed), capture-displaced.jpg (same framing with one tier product shifted). Use Canvas/OffscreenCanvas in test setup script or committed synthetic images — must be same resolution and aligned viewpoint for CI pass criteria per D-17/D-18.

    Create src/workers/visionWorker.integration.test.ts: load fixtures, call worker analyze with DEFAULT_SPLIT_Y from constants, toleranceValue 50. Assert capture-missing yields at least one type MISSING anomaly; capture-displaced yields at least one type MOVED; baseline-aligned vs itself yields zero non-dismissed anomalies. Assert no import of INITIAL_MOCK_ANOMALIES in vision.ts production path.
  </action>
  <verify>
    <automated>grep -v '^#' src/lib/vision.ts | grep -c 'INITIAL_MOCK_ANOMALIES' | grep -qx '0'</automated>
    <fails_when>mock anomalies still referenced in vision.ts</fails_when>
    <automated>grep -c 'visionWorker' src/lib/vision.ts src/workers/visionWorker.ts | grep -qv '^0$'</automated>
    <fails_when>worker not wired in vision pipeline</fails_when>
    <automated>bun run test -- src/workers/visionWorker.integration.test.ts -x</automated>
    <fails_when>golden fixture diff integration test fails or file missing</fails_when>
    <automated>bun run build</automated>
    <fails_when>Vite production build fails on OpenCV worker chunk (Pitfall 1)</fails_when>
  </verify>
  <acceptance_criteria>
    - analyzeShelfCapture dispatches to Web Worker and returns real contour-derived anomalies
    - Golden capture-missing.jpg produces ≥1 MISSING; capture-displaced.jpg produces ≥1 MOVED
    - INITIAL_MOCK_ANOMALIES removed from production vision path
    - bun run build succeeds with worker chunk emitted
  </acceptance_criteria>
  <done>Single inspect analysis path runs real OpenCV per-tier diff on golden fixtures; integration test green; production build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Pure vision helper unit tests (D-27, VIS-02, VIS-03 mapping)</name>
  <files>src/lib/vision/toleranceParams.test.ts, src/lib/vision/bboxUtils.test.ts, src/lib/vision/classifyContour.test.ts</files>
  <read_first>
    src/lib/blobUtils.test.ts
    src/lib/constants.ts
    src/types.ts
    src/lib/vision/toleranceParams.ts
    src/lib/vision/bboxUtils.ts
  </read_first>
  <behavior>
    - toleranceToDiffParams(0) → diffThreshold 60, minContourArea 200, displacementThresholdPx 8 (strict end)
    - toleranceToDiffParams(50) → mid lerp values per D-12
    - toleranceToDiffParams(100) → diffThreshold 15, minContourArea 1200, displacementThresholdPx 35 (loose end)
    - toleranceToDiffParams clamps out-of-range inputs to 0–100
    - legacyToleranceToNumber maps strict→25, normal→50, loose→75 per D-11
    - pixelRectToNormalized on known 1080×1920 rect matches INITIAL_MOCK_ANOMALIES bbox fractions within 0.001
    - stableAnomalyId is deterministic for same tier+bbox
  </behavior>
  <action>
    Create toleranceParams.test.ts, bboxUtils.test.ts following blobUtils.test.ts describe/it structure. Add classifyContour.test.ts for extracted MISSING vs MOVED heuristic functions if logic is in separate module; otherwise test classification via exported pure helpers from worker-refactored modules.

    Do not duplicate integration coverage — unit tests stay pure without WASM.
  </action>
  <verify>
    <automated>bun run test -- src/lib/vision/toleranceParams.test.ts src/lib/vision/bboxUtils.test.ts src/lib/vision/classifyContour.test.ts -x</automated>
    <fails_when>vision helper unit tests fail or files missing</fails_when>
  </verify>
  <acceptance_criteria>
    - All tolerance lerp endpoints match D-12 formulas
    - Bbox normalization outputs 0.0–1.0 coordinates matching DetectedAnomaly contract
    - Legacy enum migration helper returns 25/50/75
  </acceptance_criteria>
  <done>Pure vision helper unit tests pass; tolerance mapping and bbox contract locked before App FSM work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main thread → Worker postMessage | Untrusted bitmap dimensions and toleranceValue cross into WASM heap |
| Worker → Main thread response | Anomaly titles and bbox coords consumed by React overlay |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-01 | Denial of Service | visionWorker message handler | high | mitigate | Validate toleranceValue 0–100, splits length 4, bitmap width/height ≤ 1080×1920 before Mat ops; try/catch → error postMessage |
| T-4-02 | Tampering | npm install @techstark/opencv-js | high | mitigate | Package Legitimacy Audit OK in RESEARCH.md; checkpoint:decision D-19 before install |
| T-4-03 | Denial of Service | cv.Mat lifecycle | medium | mitigate | try/finally .delete() on every Mat/MatVector per tier loop |
| T-4-04 | Information Disclosure | Anomaly title strings | low | accept | Titles generated from tier index + bbox hash — not user HTML (RESEARCH Security) |

</threat_model>

<verification>
bun run test -- src/workers/visionWorker.integration.test.ts src/lib/vision/toleranceParams.test.ts src/lib/vision/bboxUtils.test.ts -x
bun run build
grep -v '^#' src/lib/vision.ts | grep -c 'INITIAL_MOCK_ANOMALIES' | grep -qx '0'
</verification>

<success_criteria>
Mock vision fully replaced; worker integration test proves MISSING and MOVED on golden fixtures; pure helper tests green; production build includes worker chunk.
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| src/workers/visionWorker.ts | 04-01 | OpenCV WASM per-tier diff in Web Worker |
| src/lib/vision/*.ts | 04-01 | Pure tolerance, geometry, bbox, stats helpers |
| public/test-fixtures/*.jpg | 04-01 | CI golden aligned/missing/displaced pairs |
| src/workers/visionWorker.integration.test.ts | 04-01 | End-to-end diff smoke test |

<output>
Create `.planning/phases/04-real-inspection-pipeline/04-01-SUMMARY.md` when done
</output>

<!-- Multi-Source Coverage Audit (04-01 slice) -->
<!-- TECH-06, VIS-01, VIS-02, VIS-04, RSLT-01 (real boxes from worker) — COVERED in 04-01 -->
<!-- Remaining REQ IDs assigned to 04-02 and 04-03 -->
