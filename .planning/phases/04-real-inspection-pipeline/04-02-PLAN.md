---
phase: 04-real-inspection-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/App.tsx
  - src/components/RoiSetupView.tsx
  - src/components/ResultInspectView.tsx
  - src/types.ts
  - src/types/persisted.ts
  - src/lib/storage.ts
  - src/lib/vision.ts
  - src/components/ResultInspectView.tolerance.test.tsx
  - src/App.toleranceReDiff.integration.test.tsx
  - src/lib/vision/toleranceParams.test.ts
autonomous: false
requirements:
  - VIS-03
  - ROI-01
  - ROI-02
  - ROI-03
  - ROI-04
  - ROI-05
  - RSLT-01
  - RSLT-02
  - RSLT-03
  - RSLT-04
  - RSLT-05
estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - First shutter on shelf without persisted baseline routes to ROI_CONFIG with captured frame — no SCANNING_ANIM, no diff (D-01, ROI-01)
    - RoiSetupView background shows just-captured pending baseline image — not DEFAULT_CALIBRATION CDN (D-02, ROI-01)
    - ROI save persists JPEG Blob + splitYPercentages + imageDimensions via saveBaseline; hasPersistedBaseline becomes true; returns CAMERA_IDLE (D-03, ROI-05)
    - Follow-up shutter on persisted baseline runs SCANNING_ANIM → worker diff → RESULT_INSPECT with PROCESSING fallback (D-04)
    - Continuous tolerance slider 0–100 replaces 3-button enum UI with preset ticks at 25/50/75 (D-11, D-14, RSLT-05)
    - Tolerance change debounced 150ms re-invokes full worker diff — not mock filter (D-13, VIS-03)
    - IndexedDB tolerance migrates strict→25, normal→50, loose→75 on load (D-11)
    - Blink compare long-press hides boxes and swaps to baseline image (D-24, RSLT-02)
    - Tap-to-dismiss decrements stat capsule counts immediately (D-23, RSLT-03)
    - Stat capsule filter highlights MISSING vs MOVED types (D-25, RSLT-04)
    - Retake on first-baseline flow discards pending capture and returns to CAMERA_IDLE (04-UI-SPEC ROI-05)
    - Zero anomalies shows noMissing/noDisplaced copy in stat chips (04-UI-SPEC empty state)
    - Tolerance slider remains interactive during re-diff; counts update on worker result (04-UI-SPEC loading tolerance-slider)
  artifacts:
    - src/App.tsx
    - src/components/RoiSetupView.tsx
    - src/components/ResultInspectView.tsx
    - src/lib/storage.ts
    - src/components/ResultInspectView.tolerance.test.tsx
    - src/App.toleranceReDiff.integration.test.tsx
  key_links:
    - handleShutterClick branches on hasPersistedBaseline before setAppMode SCANNING_ANIM
    - handleToleranceChange debounced → analyzeShelfCapture(capturedFrame, baseline, toleranceValue)
    - handleSaveRoiCalibration → saveBaseline with pending capture image
    - handleDismissAnomaly uses computeComplianceStats from vision helpers
---

# Plan 04-02: First-Baseline FSM, ROI Flow & Continuous Tolerance

**As a** retail field rep setting up or re-inspecting a shelf,
**I want to** calibrate ROI dividers on my first capture and adjust tolerance with a continuous slider that re-runs real diff,
**so that** my baseline is accurate and I can tune sensitivity without mock filtering.

<objective>
Wire first-baseline FSM intercept (D-01–D-05), ROI setup on pending capture (ROI-01–ROI-05), migrate tolerance enum to numeric slider with storage migration (D-11–D-14), and retain result interactions on real worker data (RSLT-01–RSLT-05).

Purpose: Complete user-facing inspection loop beyond raw worker — calibration + interactive result review.
Output: App FSM branch, RoiSetupView first-baseline UX, ResultInspectView slider, tolerance persistence migration, component tests.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-real-inspection-pipeline/04-CONTEXT.md
@.planning/phases/04-real-inspection-pipeline/04-UI-SPEC.md
@.planning/phases/04-real-inspection-pipeline/04-PATTERNS.md
@.planning/phases/04-real-inspection-pipeline/04-01-SUMMARY.md
@src/App.tsx
@src/components/RoiSetupView.tsx
@src/components/ResultInspectView.tsx
@src/lib/storage.ts
@src/types.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Confirm tolerance storage migration to numeric 0–100 (D-11)</name>
  <read_first>
    .planning/phases/04-real-inspection-pipeline/04-CONTEXT.md
    src/types.ts
    src/lib/storage.ts
  </read_first>
  <decision>
    Replace ToleranceLevel enum writes with integer toleranceValue 0–100 in IndexedDB key shelfguard_tolerance. Migrate legacy strict→25, normal→50, loose→75 on load. AuditRecord.tolerance accepts number | legacy string for read compat.
  </decision>
  <context>
    D-11 is costly/one-way for stored audits and global settings. Reversal requires reverse migration and UI rollback. Continuous slider is PRD RSLT-05 requirement — no discrete-only fallback.
  </context>
  <options>
    <option id="A">Proceed with numeric migration and continuous slider per D-11–D-14</option>
    <option id="B">Keep enum — blocked; violates RSLT-05 and locked D-11</option>
  </options>
  <resume-signal>Type "A" or "approved" to proceed with tolerance migration</resume-signal>
  <acceptance_criteria>
    - Human confirms numeric tolerance migration before storage.ts changes land
  </acceptance_criteria>
</task>

<task type="auto">
  <name>First-baseline FSM intercept and ROI setup on pending capture (D-01–D-05, ROI-01–ROI-05)</name>
  <reversibility rating="costly">handleShutterClick first-baseline branch becomes contract for empty-shelf UX across Phase 5 (D-01).</reversibility>
  <files>src/App.tsx, src/components/RoiSetupView.tsx</files>
  <read_first>
    src/App.tsx
    src/components/RoiSetupView.tsx
    .planning/phases/04-real-inspection-pipeline/04-UI-SPEC.md
    .planning/phases/04-real-inspection-pipeline/04-PATTERNS.md
  </read_first>
  <action>
    Add pendingBaselineImageUrl state in App.tsx (nullable string). At top of handleShutterClick try block after captureFrame: when hasPersistedBaseline is false, set pendingBaselineImageUrl to frame, update baseline.imageDataUrl and imageDimensions via loadImageDimensions, setAppMode ROI_CONFIG, return early — do not set SCANNING_ANIM or call analyzeShelfCapture (D-01).

    Pass isFirstBaseline={!hasPersistedBaseline && pendingBaselineImageUrl !== null} to RoiSetupView. When isFirstBaseline, show first-baseline hint copy from 04-UI-SPEC (add I18N keys tolerancePreset* optional; minimum add cn/en strings for first-baseline subtitle). Retake button (t.retake): discard pendingBaselineImageUrl, clear pending state, setAppMode CAMERA_IDLE — do not run diff (04-UI-SPEC destructive retake).

    Extend handleSaveRoiCalibration: when saving from first-baseline flow, ensure baseline.imageDataUrl is pending capture, call saveBaseline(activeShelfId, updated) with full ShelfCalibration including splitYPercentages and imageDimensions (D-03). On success register blob via objectUrlRegistry pattern from handleUploadCustomBaseline, setHasPersistedBaseline true, clear pendingBaselineImageUrl, setAppMode CAMERA_IDLE. Quota error surfaces existing banner.

    Manual ROI re-entry via onOpenRoiConfig and upload-baseline paths unchanged — background is current persisted baseline image (D-05). Existing divider drag, magnifier 80px 2×, reset splits, pointer capture patterns in RoiSetupView remain (ROI-02–ROI-04).

    Update analyzeShelfCapture calls in App to pass numeric tolerance — use tolerance state as number after migration task; interim use legacyToleranceToNumber if tolerance still enum during this task commit order.
  </action>
  <verify>
    <automated>grep -c 'pendingBaselineImageUrl' src/App.tsx | grep -qv '^0$'</automated>
    <fails_when>pending baseline state not added to App</fails_when>
    <automated>awk '/const handleShutterClick = async/,/^  };/' src/App.tsx | grep -c '!hasPersistedBaseline' | grep -qv '^0$'</automated>
    <fails_when>first-baseline guard missing inside handleShutterClick</fails_when>
    <automated>awk '/const handleShutterClick = async/,/^  };/' src/App.tsx | grep -c 'ROI_CONFIG' | grep -qv '^0$'</automated>
    <fails_when>handleShutterClick does not route to ROI_CONFIG when no persisted baseline</fails_when>
    <automated>grep -c 'isFirstBaseline' src/components/RoiSetupView.tsx src/App.tsx | grep -qv '^0$'</automated>
    <fails_when>RoiSetupView first-baseline prop not wired</fails_when>
  </verify>
  <acceptance_criteria>
    - Empty shelf first shutter opens ROI_CONFIG without scan overlay
    - ROI background is captured frame not DEFAULT_CALIBRATION
    - Save calibration persists baseline and returns to camera with hasPersistedBaseline true
    - Retake returns to camera discarding pending capture
    - Manual recalibrate on persisted baseline still works
  </acceptance_criteria>
  <done>First-baseline capture-to-calibration flow works end-to-end; persisted baseline unlocks normal inspect path.</done>
</task>

<task type="auto" tdd="true">
  <name>Continuous tolerance slider, storage migration, and result interaction wiring (D-11–D-14, D-23–D-25, VIS-03, RSLT-01–RSLT-05)</name>
  <reversibility rating="costly">Numeric tolerance in IndexedDB and AuditRecord changes read/write contract (D-11).</reversibility>
  <files>src/App.tsx, src/components/ResultInspectView.tsx, src/types.ts, src/types/persisted.ts, src/lib/storage.ts, src/lib/vision.ts, src/components/ResultInspectView.tolerance.test.tsx, src/App.toleranceReDiff.integration.test.tsx, src/lib/vision/toleranceParams.test.ts</files>
  <read_first>
    src/components/ResultInspectView.tsx
    src/components/CameraView.tsx
    src/lib/storage.ts
    src/lib/vision/complianceStats.ts
    .planning/phases/04-real-inspection-pipeline/04-UI-SPEC.md
  </read_first>
  <behavior>
    - ResultInspectView renders input type range min 0 max 100 — not three tolerance buttons
    - Slider change calls onToleranceChange with numeric value
    - Preset tick labels visible at 25, 50, 75
    - loadSavedTolerance returns 50 for missing key; migrates strict→25, normal→50, loose→75
    - After initial inspect, slider change debounced 150ms calls analyzeShelfCapture again with updated toleranceValue (VIS-03, D-13)
  </behavior>
  <action>
    Migrate types: add ToleranceValue = number; AuditRecord.tolerance and persisted audit accept ToleranceValue | ToleranceLevel for read compat. Update loadSavedTolerance/saveTolerance in storage.ts per D-11 migration on load with rewrite to IndexedDB.

    Change App tolerance state to number default 50. Init from loadSavedTolerance on mount. Replace handleToleranceChange with debounced 150ms wrapper (useMemo + debounce or ref timer) that saves numeric tolerance and calls analyzeShelfCapture(capturedFrame, baseline, value) — full worker re-diff per D-13, updates anomalies and all stat counts from result. Use computeComplianceStats in handleDismissAnomaly to dedupe formula per PATTERNS.md.

    In ResultInspectView replace 3-button strict/normal/loose grid (lines ~318–355) with horizontal range slider per 04-UI-SPEC §8: track h-2 bg-slate-200, thumb w-5 h-5 #006C49, value badge font-mono-numbers, preset ticks at 25/50/75 with cn/en labels. Props: tolerance number, onToleranceChange (value: number) => void.

    Retain unchanged: blink compare long-press (RSLT-02/D-24), tap-to-dismiss animation (RSLT-03/D-23), filterType stat capsule (RSLT-04/D-25), red/yellow overlay styling from real anomalies (RSLT-01). Real worker anomalies already flow from 04-01 — verify overlay renders normalized bbox as percentage positioning.

    Create ResultInspectView.tolerance.test.tsx following CameraView.ghost.test.tsx baseProps pattern. Assert range input present, three discrete tolerance buttons absent, onToleranceChange fired with number on change.

    Create src/App.toleranceReDiff.integration.test.tsx following App.processing.integration.test.tsx mock stack with loadBaselineRaw returning persisted baseline. Mock analyzeShelfCapture to resolve immediately. Render App, shutter to RESULT_INSPECT, advance fake timers past 800ms scan, change tolerance slider, advance debounce 150ms, assert analyzeShelfCapture called twice — second call receives updated numeric toleranceValue per D-13 (VIS-03 re-diff trigger, not client-side filter).
  </action>
  <verify>
    <automated>bun run test -- src/components/ResultInspectView.tolerance.test.tsx -x</automated>
    <fails_when>tolerance slider component tests fail</fails_when>
    <automated>bun run test -- src/App.toleranceReDiff.integration.test.tsx -x</automated>
    <fails_when>slider change does not re-invoke analyzeShelfCapture with updated tolerance</fails_when>
    <automated>grep -v '^#' src/components/ResultInspectView.tsx | grep -c "tolerance === 'strict'" | grep -qx '0'</automated>
    <fails_when>legacy 3-button tolerance UI still present</fails_when>
    <automated>grep -c 'type="range"' src/components/ResultInspectView.tsx | grep -qv '^0$'</automated>
    <fails_when>continuous tolerance slider not implemented</fails_when>
    <automated>bun run test -- src/lib/vision/toleranceParams.test.ts -x</automated>
    <fails_when>tolerance migration helper tests regress</fails_when>
  </verify>
  <acceptance_criteria>
    - Slider 0–100 replaces enum buttons with preset ticks at 25/50/75
    - Tolerance persistence migrates legacy enum values on load
    - Debounced slider change triggers worker re-diff and updates counts
    - Blink compare, tap-dismiss, and filter interactions work with real anomalies
    - Zero-anomaly result shows documented empty copy in stat chips
  </acceptance_criteria>
  <done>Continuous tolerance slider live with storage migration; result interactions operate on real worker anomaly data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tolerance slider input | User-controlled 0–100 crosses into worker diffThreshold params |
| First-baseline capture frame | Pending JPEG held in memory until save or retake |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-05 | Tampering | loadSavedTolerance migration | medium | mitigate | Validate typeof number and 0–100 range before accept; rewrite legacy strings once |
| T-4-06 | Denial of Service | Debounced re-diff storm | medium | mitigate | 150ms debounce on slider; ignore overlapping analyzeShelfCapture if prior in-flight (optional abort) |
| T-4-07 | Information Disclosure | Pending baseline in memory | low | accept | Cleared on save or retake; not persisted until saveBaseline |

</threat_model>

<verification>
bun run test -- src/components/ResultInspectView.tolerance.test.tsx src/App.toleranceReDiff.integration.test.tsx src/lib/vision/toleranceParams.test.ts -x
grep -c 'type="range"' src/components/ResultInspectView.tsx | grep -qv '^0$'
grep -c '!hasPersistedBaseline' src/App.tsx | grep -qv '^0$'
</verification>

<success_criteria>
First capture calibrates ROI without scan; follow-up capture shows real diff results; tolerance slider re-runs worker diff; result interactions retained on real data.
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| src/App.tsx (FSM + tolerance) | 04-02 | First-baseline intercept and debounced re-diff |
| src/components/RoiSetupView.tsx | 04-02 | First-baseline hint and retake |
| src/components/ResultInspectView.tsx | 04-02 | Continuous tolerance slider |
| src/lib/storage.ts | 04-02 | Numeric tolerance migration |
| src/components/ResultInspectView.tolerance.test.tsx | 04-02 | Slider UI regression tests |

<output>
Create `.planning/phases/04-real-inspection-pipeline/04-02-SUMMARY.md` when done
</output>

<!-- Multi-Source Coverage Audit (04-02 slice) -->
<!-- VIS-03, ROI-01–ROI-05, RSLT-01–RSLT-05 — COVERED in 04-02 -->
<!-- D-01–D-05, D-11–D-14, D-23–D-25 — COVERED in 04-02 tasks -->
