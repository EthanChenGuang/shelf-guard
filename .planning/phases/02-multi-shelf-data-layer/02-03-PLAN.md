---
phase: 02-multi-shelf-data-layer
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - src/lib/constants.ts
  - src/components/CameraView.tsx
  - src/components/CameraView.quota.test.tsx
  - src/App.tsx
  - src/lib/shelfStorage.ts
  - src/lib/shelfStorage.test.ts
autonomous: true
requirements:
  - DATA-04
  - DATA-05
  - TECH-04
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - QuotaExceededError shows inline banner on camera view, not console-only (D-18, DATA-04)
    - Failed writes blocked and surfaced via quotaError state (D-19)
    - Banner copy in cn/en via I18N constants (D-21)
    - Audit thumbnails compressed 320px JPEG q=0.75 before persist (D-12)
    - Per-shelf history capped at 20 FIFO on insert (D-11, DATA-05)
    - Migration write failure surfaces quota banner (D-20)
  artifacts:
    - src/components/CameraView.quota.test.tsx
    - src/lib/constants.ts
  key_links:
    - App passes quotaError and onDismissQuotaError to CameraView
    - appendAuditRecord calls compressToJpegBlob before persist
    - CameraView quota banner mirrors camera error banner layout
  prohibitions:
    - Must not silently swallow QuotaExceededError on write paths
    - Must not add delete-history management UI (deferred)
---

# Plan 02-03: Quota UX, History Cap & Blob Persist Polish

**As a** retail field rep using ShelfGuard daily,
**I want to** see a clear message when storage is full and keep history bounded per shelf,
**so that** the app stays usable without silently losing my inspection data.

<objective>
Complete Phase 2 data layer: quota-exceeded inline banner, thumbnail compression on audit append, FIFO history enforcement in live audit flow, and full-suite verification gate.

Purpose: Satisfy DATA-04, DATA-05, and remaining TECH-04 app-layer persistence requirements.
Output: Quota banner UI + tests, compressed thumbnail pipeline, phase verification green.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-multi-shelf-data-layer/02-CONTEXT.md
@.planning/phases/02-multi-shelf-data-layer/02-PATTERNS.md
@.planning/phases/02-multi-shelf-data-layer/02-02-SUMMARY.md
@src/components/CameraView.tsx
@src/components/CameraView.error.test.tsx
@src/lib/shelfStorage.ts
@src/App.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Quota-exceeded inline banner on CameraView (D-18, D-19, D-21, DATA-04)</name>
  <files>src/lib/constants.ts, src/components/CameraView.tsx, src/components/CameraView.quota.test.tsx, src/App.tsx</files>
  <read_first>
    src/components/CameraView.tsx
    src/components/CameraView.error.test.tsx
    src/lib/constants.ts
    .planning/phases/02-multi-shelf-data-layer/02-CONTEXT.md
  </read_first>
  <behavior>
    - CameraView renders quotaExceededTitle when quotaError=true
    - Dismiss button calls onDismissQuotaError
    - App sets quotaError when migration, saveBaseline, or appendAuditRecord returns QUOTA_EXCEEDED
    - Failed write does not update React state as if succeeded
  </behavior>
  <action>
    Add I18N keys quotaExceededTitle and quotaExceededGuide to both cn and en blocks in constants.ts per D-21 (suggest clearing history on other shelves; no delete UI).

    Extend CameraViewProps with quotaError?: boolean and onDismissQuotaError?: () => void. Render inline banner duplicating camera error banner structure (role=alert, amber border, AlertCircle, dismiss X with aria-label t.close) when quotaError is true. Place adjacent to camera error banner in bottom control area.

    In App.tsx: add quotaError boolean state and setQuotaError. On runSchemaMigrationIfNeeded failure set quotaError true and skip hydration. On saveBaseline/appendAuditRecord/clearBaseline StorageWriteResult failure set quotaError; do not update baseline/auditHistory state on failed write (D-19 block failed write).

    Create src/components/CameraView.quota.test.tsx mirroring CameraView.error.test.tsx pattern: render with quotaError=true asserts cn title text; dismiss fires onDismissQuotaError.
  </action>
  <acceptance_criteria>
    - quotaExceededTitle and quotaExceededGuide exist in cn and en I18N
    - CameraView shows quota banner when quotaError true
    - App does not update state when storage returns QUOTA_EXCEEDED
    - CameraView.quota.test.tsx passes
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/components/CameraView.quota.test.tsx</automated>
    <fails_when>quota component tests fail</fails_when>
    <automated>grep -c 'quotaExceededTitle' src/lib/constants.ts | grep -qv '^0$'</automated>
    <fails_when>I18N quota keys missing</fails_when>
    <automated>grep -c 'quotaError' src/App.tsx src/components/CameraView.tsx | grep -qv '^0$'</automated>
    <fails_when>quotaError prop wiring missing</fails_when>
  </verify>
  <done>DATA-04 satisfied: QuotaExceededError surfaces inline banner; writes blocked on quota failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Thumbnail compression and FIFO history cap in audit flow (D-11, D-12, D-13, DATA-05)</name>
  <files>src/lib/shelfStorage.ts, src/lib/shelfStorage.test.ts, src/App.tsx</files>
  <read_first>
    src/lib/shelfStorage.ts
    src/lib/blobUtils.ts
    src/App.tsx
    .planning/phases/02-multi-shelf-data-layer/02-CONTEXT.md
  </read_first>
  <behavior>
    - appendAuditRecord compresses thumbnail to max 320px JPEG q=0.75 before persist
    - 21st insert on same shelf evicts oldest record (length stays 20)
    - Persisted audit records store thumbnailBlob not data URL string
  </behavior>
  <action>
    Ensure appendAuditRecord in shelfStorage.ts always runs compressToJpegBlob on thumbnail source (data URL or Blob) before building PersistedAuditRecord with thumbnailBlob (D-08, D-12).

    Enforce FIFO: updated = [record, ...existing].slice(0, HISTORY_CAP) with HISTORY_CAP=20 (D-11, D-13).

    Update handleCompleteAudit in App.tsx: pass capturedFrame to appendAuditRecord; storage layer handles compression. After successful append, prepend to auditHistory state with resolved thumbnailUrl from registry.

    Extend shelfStorage.test.ts: test 21 appends yields 20 records; test persisted thumbnail is Blob; test dimensions after compress do not exceed 320px width (mock canvas dimensions).

    Add unit test that get from idb-keyval for shelf:0:history returns array length <= 20 after stress append.
  </action>
  <acceptance_criteria>
    - History never exceeds 20 records per shelf after append
    - Thumbnails stored as Blob in IndexedDB
    - compressToJpegBlob invoked on audit append path
    - shelfStorage tests for cap and Blob thumbnail pass
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/lib/blobUtils.test.ts src/lib/shelfStorage.test.ts</automated>
    <fails_when>blob or shelfStorage tests fail</fails_when>
    <automated>grep -c 'compressToJpegBlob' src/lib/shelfStorage.ts | grep -qv '^0$'</automated>
    <fails_when>compression not wired in append path</fails_when>
  </verify>
  <done>DATA-05 satisfied: per-shelf history bounded at 20 with compressed JPEG thumbnails.</done>
</task>

<task type="auto">
  <name>Phase 2 verification gate — full suite and build</name>
  <files>src/lib/shelfStorage.ts, src/App.tsx</files>
  <read_first>
    .planning/phases/02-multi-shelf-data-layer/02-VALIDATION.md
    .planning/ROADMAP.md
  </read_first>
  <action>
    Verify all Phase 2 success criteria from ROADMAP: (1) shelf A data not on shelf B — integration test; (2) active shelf restored on reopen — integration test; (3) legacy migrates to shelf 0 — unit test; (4) quota banner — component test; (5) history cap 20 with thumbnails — unit test.

    Confirm saveBaseline persists JPEG Blob not string: add or extend shelfStorage.test.ts assertion reading raw idb-keyval value for shelf:0:baseline and checking imageBlob instanceof Blob.

    Run bun run lint, bun run build, bun run test full suite. Fix any regressions in Phase 1 tests caused by storage import path changes.

    Update 02-VALIDATION.md task status rows to green if executor convention allows; otherwise note in SUMMARY.
  </action>
  <acceptance_criteria>
    - Full test suite passes
    - Build succeeds
    - All 9 phase requirement IDs have automated coverage via tests listed in 02-VALIDATION.md
  </acceptance_criteria>
  <verify>
    <automated>bun run lint && bun run build && bun run test</automated>
    <fails_when>lint, build, or any test fails</fails_when>
    <automated>bun run test -- src/App.shelfIsolation.integration.test.tsx src/lib/shelfStorage.test.ts src/components/CameraView.quota.test.tsx</automated>
    <fails_when>Phase 2 targeted tests fail</fails_when>
  </verify>
  <done>Phase 2 complete: TECH-04, DATA-01 through DATA-05, SHLF-02 through SHLF-04 verified by automated tests and build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User device storage quota | IndexedDB write failures cross into UI |
| CameraView presentation | Quota message must not expose internal stack traces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Denial of Service | Uncompressed audit thumbnails | medium | mitigate | compressToJpegBlob 320px q=0.75 + FIFO cap 20 (D-11/D-12) |
| T-02-08 | Denial of Service | Silent quota swallow | high | mitigate | StorageWriteResult + quota banner; block state update (D-19) |
| T-02-09 | Information Disclosure | Quota banner copy | low | accept | User-facing guidance only; no internal paths in message |

</threat_model>

<verification>
bun run lint && bun run build && bun run test
</verification>

<success_criteria>
Quota banner visible on QUOTA_EXCEEDED; history capped at 20 with compressed thumbnails; full suite and build green; all Phase 2 ROADMAP success criteria met.
</success_criteria>

## Artifacts this phase produces

- src/components/CameraView.quota.test.tsx — DATA-04 automated UI verification
- Quota I18N strings in constants.ts — cn/en quotaExceededTitle, quotaExceededGuide
- Compressed thumbnail pipeline in appendAuditRecord — DATA-05 enforcement

<output>
Create `.planning/phases/02-multi-shelf-data-layer/02-03-SUMMARY.md` when done
</output>
