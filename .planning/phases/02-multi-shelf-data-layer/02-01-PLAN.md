---
phase: 02-multi-shelf-data-layer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.setup.ts
  - src/types/persisted.ts
  - src/lib/blobUtils.ts
  - src/lib/blobUtils.test.ts
  - src/lib/shelfStorage.ts
  - src/lib/shelfStorage.test.ts
autonomous: false
requirements:
  - TECH-04
  - DATA-01
  - DATA-02
  - DATA-03
  - SHLF-04
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Legacy shelfguard_baseline and shelfguard_audit_history migrate to shelf index 0 with schema_version=2 (D-01, D-02, D-03, D-04)"
    - "Re-running migration when schema_version=2 is a no-op (idempotent)"
    - "Baseline and audit thumbnails persist as JPEG Blob fields, not base64 strings (D-05, D-08)"
    - "shelfId 0-4 validated on every shelf-scoped read/write"
    - "activeShelfId loads/saves via shelfguard_active_shelf key defaulting to 0 (SHLF-04, D-09)"
    - "Storage writes return QUOTA_EXCEEDED structured error on QuotaExceededError (D-19)"
  artifacts:
    - src/lib/shelfStorage.ts
    - src/lib/blobUtils.ts
    - src/types/persisted.ts
    - src/lib/shelfStorage.test.ts
  key_links:
    - runSchemaMigrationIfNeeded runs before App hydration (called by App in plan 02-02)
    - shelfStorage uses idb-keyval setMany/delMany for atomic migration (D-02, D-03)
  prohibitions:
    - Must not dual-read legacy keys in steady state after migration (D-03)
    - Must not store base64 data URLs in IndexedDB after migration pass (D-06)
---

# Plan 02-01: Storage Foundation — Blob Utils, Persisted Types & Shelf CRUD

**As a** retail field rep managing multiple fixtures,
**I want to** have five isolated shelf datasets stored efficiently in IndexedDB,
**so that** my baseline and inspection history never mix between shelves.

<objective>
Establish Wave 0 storage infrastructure: fake-indexeddb test harness, Blob conversion helpers, persisted types, and shelfStorage.ts with v1→v2 migration and shelf-scoped CRUD API.

Purpose: Provide the storage contract (D-17) that App.tsx and integration tests consume in plans 02-02 and 02-03.
Output: shelfStorage.ts, blobUtils.ts, persisted types, and unit tests covering migration, isolation, cap, quota, and active shelf.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-multi-shelf-data-layer/02-CONTEXT.md
@.planning/phases/02-multi-shelf-data-layer/02-RESEARCH.md
@.planning/phases/02-multi-shelf-data-layer/02-PATTERNS.md
@.planning/phases/02-multi-shelf-data-layer/02-VALIDATION.md
@src/lib/storage.ts
@src/types.ts
@vitest.setup.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Install fake-indexeddb and scaffold blobUtils + persisted types</name>
  <files>package.json, vitest.setup.ts, src/types/persisted.ts, src/lib/blobUtils.ts, src/lib/blobUtils.test.ts</files>
  <read_first>
    package.json
    vitest.setup.ts
    src/lib/imageDimensions.ts
    .planning/phases/02-multi-shelf-data-layer/02-RESEARCH.md
    .planning/phases/02-multi-shelf-data-layer/02-PATTERNS.md
  </read_first>
  <action>
    Add devDependency fake-indexeddb@6.2.5 (RESEARCH Package Legitimacy Audit verdict OK). Prepend import fake-indexeddb/auto to vitest.setup.ts before jest-dom import so storage tests have IndexedDB.

    Create src/types/persisted.ts with PersistedBaseline (imageBlob: Blob plus metadata fields matching ShelfCalibration minus imageDataUrl) and PersistedAuditRecord (thumbnailBlob: Blob instead of thumbnailUrl, all other AuditRecord fields unchanged). Export StorageWriteResult union type { ok: true } | { ok: false; error: 'QUOTA_EXCEEDED' }.

    Create src/lib/blobUtils.ts exporting: dataUrlToBlob via fetch(dataUrl).then(r => r.blob()); isQuotaError checking err instanceof DOMException && err.name === 'QuotaExceededError'; compressToJpegBlob(source, maxWidth=320, quality=0.75) using Canvas drawImage + toBlob image/jpeg per D-12.

    Create src/lib/blobUtils.test.ts with tests for isQuotaError with DOMException QuotaExceededError; compressToJpegBlob output type image/jpeg (stub HTMLCanvasElement.prototype.toBlob in jsdom if needed). Match naming/export style from imageDimensions.ts.
  </action>
  <acceptance_criteria>
    - fake-indexeddb listed in package.json devDependencies
    - vitest.setup.ts imports fake-indexeddb/auto
    - PersistedBaseline and PersistedAuditRecord exported from src/types/persisted.ts
    - blobUtils.test.ts passes with isQuotaError and compress smoke tests
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'fake-indexeddb' package.json | grep -qv '^0$'</automated>
    <fails_when>fake-indexeddb not in package.json</fails_when>
    <automated>grep -c 'fake-indexeddb/auto' vitest.setup.ts | grep -qv '^0$'</automated>
    <fails_when>vitest.setup.ts missing fake-indexeddb import</fails_when>
    <automated>test -f src/types/persisted.ts && test -f src/lib/blobUtils.ts && bun run test -- src/lib/blobUtils.test.ts</automated>
    <fails_when>types or blobUtils missing or tests fail</fails_when>
  </verify>
  <done>Wave 0 test harness and Blob helpers exist; persisted types define storage boundary per D-05/D-08.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Confirm one-way schema v2 and shelfStorage API contract (D-04, D-17)</name>
  <read_first>
    .planning/phases/02-multi-shelf-data-layer/02-CONTEXT.md
    .planning/phases/02-multi-shelf-data-layer/02-RESEARCH.md
  </read_first>
  <decision>
    Confirm irreversible schema v2 migration (D-04) and shelfStorage.ts as the multi-phase storage API contract (D-17) before implementation begins.
  </decision>
  <options>
    <option id="A" recommended="true">Embedded Blob in composite keys at namespaced shelf baseline and history keys; HISTORY_CAP=20; global lang/tolerance stay in storage.ts</option>
    <option id="B">Split blob keys (separate baseline blob key per shelf) — higher complexity; only if profiling demands metadata-only updates</option>
  </options>
  <action>
    Present D-04 one-way risk (legacy keys deleted after migration; downgrade without reverse migration corrupts data) and D-17 costly contract (Phases 3-5 depend on shelfStorage API shape). Ask human to pick option A or B. Default recommendation is A per RESEARCH Pattern 1.
  </action>
  <acceptance_criteria>
    - Human selects option A or B before next task executes
    - Decision recorded in commit message or SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/02-multi-shelf-data-layer/02-01-PLAN.md</automated>
    <fails_when>plan file missing — checkpoint cannot resume</fails_when>
  </verify>
  <resume-signal>Reply with "A" or "B" (or "approved" to accept recommended option A)</resume-signal>
  <done>Human approved schema v2 migration approach and shelfStorage API contract per D-04 and D-17.</done>
</task>

<task type="auto" tdd="true">
  <name>Implement shelfStorage migration and shelf-scoped CRUD (DATA-01, DATA-02, DATA-03, TECH-04)</name>
  <files>src/lib/shelfStorage.ts, src/lib/shelfStorage.test.ts</files>
  <read_first>
    src/lib/storage.ts
    src/types/persisted.ts
    src/lib/blobUtils.ts
    src/lib/constants.ts
    .planning/phases/02-multi-shelf-data-layer/02-CONTEXT.md
    .planning/phases/02-multi-shelf-data-layer/02-RESEARCH.md
  </read_first>
  <reversibility rating="one-way">D-04 schema_version=2 and legacy key deletion cannot be safely reversed without a dedicated downgrade migration.</reversibility>
  <behavior>
    - Migration: legacy baseline + history at shelfguard_* keys move to shelf:0:*, schema_version set to 2, legacy keys deleted
    - Idempotency: second runSchemaMigrationIfNeeded when version=2 is no-op
    - Isolation: saveBaseline(0) data not returned by loadBaseline(1)
    - Empty shelves: loadBaseline(1) through loadBaseline(4) return DEFAULT_CALIBRATION after migration with no user data (D-10)
    - Active shelf: saveActiveShelfId(3) then loadActiveShelfId returns 3; invalid values clamp to 0–4
    - History cap: append 21 records on shelf 0 yields length 20 FIFO (oldest dropped)
    - Quota: mocked set throwing QuotaExceededError returns { ok: false, error: 'QUOTA_EXCEEDED' }
    - Blob storage: persisted baseline imageBlob is Blob instance, not string
  </behavior>
  <action>
    Implement src/lib/shelfStorage.ts per approved checkpoint (default option A). Key builders: shelf:{0-4}:baseline, shelf:{0-4}:history, shelfguard_active_shelf, shelfguard_schema_version=2. validateShelfId clamps 0–4.

    runSchemaMigrationIfNeeded: if get(schema_version)===2 return { ok: true }. Read legacy shelfguard_baseline and shelfguard_audit_history. Convert imageDataUrl/thumbnailUrl strings to Blob via dataUrlToBlob in same pass (D-06). Validate splitYPercentages.length===4 before migrating baseline. Build entries array with schema_version, shelf:0:baseline, shelf:0:history. await setMany(entries) then delMany legacy keys (D-02, D-03). On QuotaExceeded return { ok: false, error: 'QUOTA_EXCEEDED' } (D-19, D-20 migration path).

    loadBaseline(shelfId): read PersistedBaseline, validate, return ShelfCalibration via toViewBaseline — caller supplies displayUrl in plan 02-02; for unit tests pass blob URL or empty string. Read failures: console.warn + DEFAULT_CALIBRATION.

    saveBaseline(shelfId, viewCalibration): convert view imageDataUrl to imageBlob via dataUrlToBlob if string, persist PersistedBaseline, return StorageWriteResult.

    loadAuditHistoryRaw / appendAuditRecord: FIFO cap HISTORY_CAP=20 per D-11/D-13; thumbnails as thumbnailBlob; compress via compressToJpegBlob on append when source is data URL.

    loadActiveShelfId / saveActiveShelfId: integer 0–4 default 0.

    Export toViewBaseline and toViewAuditRecord mappers per RESEARCH Pattern 3.

    Write src/lib/shelfStorage.test.ts covering migration DATA-03, idempotent skip, shelf isolation DATA-01, Blob not string DATA-02, cap DATA-05 partial, quota DATA-04, active shelf SHLF-04. Use fake-indexeddb clear in beforeEach.
  </action>
  <acceptance_criteria>
    - runSchemaMigrationIfNeeded migrates legacy data to shelf:0 and sets schema_version=2
    - Second migration call does not rewrite or duplicate data
    - saveBaseline/loadBaseline isolated per shelfId
    - loadBaseline(1–4) returns DEFAULT_CALIBRATION when no baseline persisted (D-10)
    - appendAuditRecord caps at 20 per shelf FIFO
    - QuotaExceededError returns structured error without silent swallow
    - bun run test -- src/lib/shelfStorage.test.ts passes all tests
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/lib/shelfStorage.test.ts</automated>
    <fails_when>any shelfStorage unit test fails</fails_when>
    <automated>grep -c 'runSchemaMigrationIfNeeded' src/lib/shelfStorage.ts | grep -qv '^0$'</automated>
    <fails_when>migration entry point missing</fails_when>
    <automated>grep -c 'HISTORY_CAP' src/lib/shelfStorage.ts | grep -qv '^0$'</automated>
    <fails_when>history cap constant not defined</fails_when>
  </verify>
  <done>shelfStorage.ts provides migration, shelf CRUD, active shelf persistence, and unit test coverage for DATA-01/02/03 and TECH-04 storage layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser IndexedDB | Untrusted quota limits; local-only user data |
| npm registry → devDependency | fake-indexeddb install |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | runSchemaMigrationIfNeeded | high | mitigate | setMany all new keys before delMany legacy; validate splitYPercentages.length===4 |
| T-02-02 | Denial of Service | saveBaseline/appendAuditRecord | medium | mitigate | isQuotaError detection; StorageWriteResult returned to App (D-19) |
| T-02-03 | Tampering | shelfId parameter | medium | mitigate | validateShelfId clamp 0–4 on every public API |
| T-02-SC | Tampering | fake-indexeddb npm install | low | accept | Package Legitimacy Audit verdict OK; dev-only dependency |

</threat_model>

<verification>
bun run test -- src/lib/blobUtils.test.ts src/lib/shelfStorage.test.ts && bun run lint
</verification>

<success_criteria>
shelfStorage migration and CRUD unit tests green; persisted types use Blob fields; active shelf and schema version keys implemented; checkpoint D-04/D-17 approved.
</success_criteria>

## Artifacts this phase produces

- src/lib/shelfStorage.ts — shelf-scoped IndexedDB API (migration, CRUD, active shelf)
- src/lib/blobUtils.ts — dataUrlToBlob, compressToJpegBlob, isQuotaError
- src/types/persisted.ts — PersistedBaseline, PersistedAuditRecord, StorageWriteResult
- vitest.setup.ts — fake-indexeddb/auto import for storage tests

<output>
Create `.planning/phases/02-multi-shelf-data-layer/02-01-SUMMARY.md` when done
</output>
