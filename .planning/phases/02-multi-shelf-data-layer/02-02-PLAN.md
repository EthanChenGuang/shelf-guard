---
phase: 02-multi-shelf-data-layer
plan: 02
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - src/lib/objectUrlRegistry.ts
  - src/lib/storage.ts
  - src/App.tsx
  - src/components/ShelfSelector.tsx
  - src/components/CameraView.tsx
  - src/App.shelfIsolation.integration.test.tsx
autonomous: true
requirements:
  - SHLF-02
  - SHLF-03
  - SHLF-04
  - DATA-01
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - App init runs migration then loads activeShelfId before shelf-scoped hydration (D-14)
    - Switching shelf B does not show shelf A baseline id or history records (SHLF-02, SHLF-03, D-16)
    - User-facing label 1 maps to shelf index 0 (D-01)
    - Shelves 1–4 (indices 1–4) start empty with DEFAULT_CALIBRATION after migration — no persisted baseline until user calibrates (D-10)
    - Object URLs revoked on shelf switch to prevent leaks (D-07)
    - Minimal 5-shelf QA selector visible on camera view (D-15)
    - activeShelfId restored on app remount from shelfguard_active_shelf (SHLF-04)
  artifacts:
    - src/App.shelfIsolation.integration.test.tsx
    - src/components/ShelfSelector.tsx
    - src/lib/objectUrlRegistry.ts
  key_links:
    - App.tsx init calls runSchemaMigrationIfNeeded then loadActiveShelfId then loadBaseline(activeShelfId)
    - handleShelfChange revokes URLs, loads new shelf data, saveActiveShelfId
    - CameraView renders ShelfSelector when onShelfChange provided
  prohibitions:
    - Must not implement PRD swipe carousel (deferred SHLF-01)
    - Must not pass raw Blob into CameraView baseline prop (D-07)
---

# Plan 02-02: Tracer — End-to-End Shelf Isolation in App

**As a** retail field rep inspecting multiple fixtures,
**I want to** switch between shelves and see only that shelf's baseline and history,
**so that** inspection data from one fixture never appears on another.

<objective>
Wire shelfStorage into App.tsx with a production-quality tracer slice: migrate legacy data, load shelf 0, switch to empty shelf 1, save baseline on shelf 1, switch back and confirm shelf 0 data intact. Add mandatory integration tests (D-16).

Purpose: Prove multi-shelf isolation end-to-end before quota and thumbnail polish in plan 02-03.
Output: App shelf wiring, objectUrlRegistry, ShelfSelector QA UI, integration test suite.
</objective>

<execution_context>
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/workflows/execute-plan.md
@/home/guang/Projects/Experiments/image-comparation/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-multi-shelf-data-layer/02-CONTEXT.md
@.planning/phases/02-multi-shelf-data-layer/02-PATTERNS.md
@.planning/phases/02-multi-shelf-data-layer/02-01-SUMMARY.md
@src/lib/shelfStorage.ts
@src/App.tsx
@src/components/CameraView.tsx
@src/App.processing.integration.test.tsx
</context>

<tasks>

<task type="tracer">
  <name>End-to-end shelf isolation — migrate, switch, save, verify (D-14, D-16)</name>
  <files>src/lib/objectUrlRegistry.ts, src/App.tsx, src/components/ShelfSelector.tsx, src/components/CameraView.tsx, src/App.shelfIsolation.integration.test.tsx</files>
  <read_first>
    src/App.tsx
    src/lib/shelfStorage.ts
    src/components/CameraView.tsx
    src/App.processing.integration.test.tsx
    .planning/phases/02-multi-shelf-data-layer/02-CONTEXT.md
    .planning/phases/02-multi-shelf-data-layer/02-PATTERNS.md
  </read_first>
  <action>
    Create src/lib/objectUrlRegistry.ts with createDisplayUrlRegistry per RESEARCH Pattern 3: set(key, blob) creates URL.createObjectURL and tracks keys; revoke(key); revokeAll().

    Create src/components/ShelfSelector.tsx: 5-button segmented control labels 1–5 mapping to indices 0–4 (D-01, D-15). Props activeShelfId, onShelfChange, lang. Active styling matches ResultInspectView tolerance button pattern from PATTERNS.md.

    Update App.tsx: add activeShelfId state (number 0–4). Init useEffect sequence: (1) runSchemaMigrationIfNeeded — if !ok set quotaError and return (quota banner wired in 02-03); (2) loadActiveShelfId + setActiveShelfId; (3) loadShelfData(activeShelfId) helper that loads baseline/history from shelfStorage, resolves Blobs to display URLs via registry, setBaseline/setAuditHistory.

    Implement handleShelfChange(newShelfId): registry.revokeAll(); setActiveShelfId; saveActiveShelfId; reload shelf data for newShelfId (D-07, D-14).

    Update all baseline/history save paths to pass activeShelfId: handleSaveRoiCalibration, handleCompleteAudit, handleResetToDefault, handleUploadCustomBaseline — use shelfStorage saveBaseline(activeShelfId), appendAuditRecord(activeShelfId), clearBaseline(activeShelfId). Check StorageWriteResult; set quotaError on QUOTA_EXCEEDED (minimal state flag even if banner UI completes in 02-03).

    Pass to CameraView: activeShelfId, onShelfChange=handleShelfChange. Render ShelfSelector in CameraView top tools row when onShelfChange provided.

    Create src/App.shelfIsolation.integration.test.tsx (D-16 mandatory): use fake-indexeddb (no mock of shelfStorage). beforeEach clear IndexedDB. Mock useCameraStream, useDeviceOrientation, usePWAInstall, vision like App.processing.integration.test.tsx. Seed shelf 0 baseline id shelf0-test and shelf 1 id shelf1-test via shelfStorage directly. Render App; assert shelf 0 baseline visible. Click selector label 2 (index 1); assert baseline id is shelf1-test not shelf0-test. Seed history on shelf 0 only; switch to shelf 1; open history modal; assert shelf 0 records not shown. Test remount: saveActiveShelfId(2), unmount/remount App; assert selector shows shelf 3 active (index 2) per SHLF-04.

    Tracer manual path encoded in integration test: migrate legacy keys seeded in beforeEach → load shelf 0 → switch shelf 1 empty DEFAULT_CALIBRATION → saveBaseline on shelf 1 → switch back → shelf 0 baseline id unchanged.
  </action>
  <acceptance_criteria>
    - Integration test proves shelf A baseline and history do not leak to shelf B (D-16)
    - activeShelfId persists across remount (SHLF-04)
    - Shelf selector labels 1–5 map to indices 0–4
    - handleShelfChange calls registry.revokeAll before loading new shelf
    - bun run test -- src/App.shelfIsolation.integration.test.tsx passes
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/App.shelfIsolation.integration.test.tsx</automated>
    <fails_when>integration test fails or file missing</fails_when>
    <automated>grep -c 'activeShelfId' src/App.tsx | grep -qv '^0$'</automated>
    <fails_when>App.tsx missing activeShelfId state</fails_when>
    <automated>grep -c 'runSchemaMigrationIfNeeded' src/App.tsx | grep -qv '^0$'</automated>
    <fails_when>App init does not call migration</fails_when>
    <automated>grep -c 'revokeAll' src/App.tsx | grep -qv '^0$'</automated>
    <fails_when>shelf switch does not revoke object URLs</fails_when>
  </verify>
  <done>Single happy path works end-to-end: legacy migration, shelf 0 load, switch to shelf 1, save baseline, switch back with shelf 0 intact; integration tests green.</done>
</task>

<task type="auto">
  <name>Refactor storage.ts to global-settings-only thin wrapper (D-17)</name>
  <files>src/lib/storage.ts</files>
  <read_first>
    src/lib/storage.ts
    src/lib/shelfStorage.ts
    .planning/phases/02-multi-shelf-data-layer/02-PATTERNS.md
  </read_first>
  <reversibility rating="costly">storage.ts becomes thin facade; shelf CRUD only via shelfStorage.ts per D-17.</reversibility>
  <action>
    Refactor src/lib/storage.ts: remove flat loadBaseline/saveBaseline/loadAuditHistory/saveAuditRecord/clearBaseline implementations. Keep loadSavedLanguage, saveLanguage, loadSavedTolerance, saveTolerance with existing KEY_LANG and KEY_TOLERANCE constants unchanged (D-09 global keys).

    Re-export shelf-scoped functions from shelfStorage.ts for convenience OR update App.tsx imports to import shelf functions from ./lib/shelfStorage and global settings from ./lib/storage — pick one consistent pattern; App must not import deprecated flat APIs.

    Add JSDoc @deprecated on any temporary wrapper if kept; prefer direct shelfStorage imports in App.tsx.

    Ensure no remaining imports of flat loadBaseline() without shelfId under src/ except tests documenting migration.
  </action>
  <acceptance_criteria>
    - storage.ts retains only global lang/tolerance functions
    - App.tsx imports shelf CRUD from shelfStorage.ts
    - No flat shelfguard_baseline key writes remain in src/
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'shelfguard_baseline' src/lib/storage.ts | grep -qx '0'</automated>
    <fails_when>storage.ts still references legacy baseline key</fails_when>
    <automated>grep -c 'loadSavedLanguage' src/lib/storage.ts | grep -qv '^0$'</automated>
    <fails_when>global language loader removed</fails_when>
    <automated>bun run test -- src/App.shelfIsolation.integration.test.tsx</automated>
    <fails_when>refactor breaks integration tests</fails_when>
  </verify>
  <done>storage.ts is thin global-settings module; shelfStorage.ts is sole shelf CRUD contract for Phases 3–5.</done>
</task>

<task type="auto">
  <name>Wire shelf-scoped history and baseline saves through audit flow</name>
  <files>src/App.tsx</files>
  <read_first>
    src/App.tsx
    src/lib/shelfStorage.ts
    src/lib/blobUtils.ts
  </read_first>
  <action>
    Ensure handleCompleteAudit uses appendAuditRecord(activeShelfId, record) and updates local auditHistory state only for active shelf. On shelf switch, auditHistory state replaces entirely from loadAuditHistory(activeShelfId) — no merge across shelves.

    Ensure handleResetToDefault calls clearBaseline(activeShelfId) scoped to active shelf only; other shelves unaffected.

    Ensure handleUploadCustomBaseline and handleSaveRoiCalibration persist via saveBaseline(activeShelfId) converting imageDataUrl to Blob at storage boundary.

    Add useEffect cleanup on App unmount: registry.revokeAll() to prevent object URL leaks (D-07).

    Run full test suite to confirm no regressions from Phase 1 FSM/camera tests.
  </action>
  <acceptance_criteria>
    - All four save handlers pass activeShelfId to shelfStorage
    - App unmount revokes all display URLs
    - Phase 1 tests still pass
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/App.shelfIsolation.integration.test.tsx src/App.capture.test.ts src/App.processing.test.ts</automated>
    <fails_when>integration or Phase 1 App tests fail</fails_when>
    <automated>grep -c 'activeShelfId' src/App.tsx | grep -qv '^0$'</automated>
    <fails_when>activeShelfId not referenced in save handlers</fails_when>
  </verify>
  <done>All baseline and history mutations are shelf-scoped; object URL lifecycle managed on switch and unmount.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App state ↔ IndexedDB | activeShelfId must match persisted shelf data on load |
| Object URL registry ↔ DOM | Display URLs must not leak across shelf switches |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Information Disclosure | objectUrlRegistry | medium | mitigate | revokeAll on shelf switch and App unmount (D-07) |
| T-02-05 | Tampering | handleShelfChange | medium | mitigate | validateShelfId in shelfStorage; clamp selector to 0–4 |
| T-02-06 | Repudiation | Missing integration test | high | mitigate | D-16 mandatory App.shelfIsolation.integration.test.tsx |

</threat_model>

<verification>
bun run test -- src/App.shelfIsolation.integration.test.tsx && bun run lint
</verification>

<success_criteria>
Tracer slice verified: shelf isolation integration tests pass; QA shelf selector works; storage.ts refactored; SHLF-02, SHLF-03, SHLF-04 satisfied at App layer.
</success_criteria>

## Artifacts this phase produces

- src/lib/objectUrlRegistry.ts — display URL create/revoke registry
- src/components/ShelfSelector.tsx — temporary 5-shelf QA control (D-15)
- src/App.shelfIsolation.integration.test.tsx — mandatory shelf isolation proof (D-16)
- App.tsx activeShelfId wiring — shelf-scoped load/save/switch handlers

<output>
Create `.planning/phases/02-multi-shelf-data-layer/02-02-SUMMARY.md` when done
</output>
